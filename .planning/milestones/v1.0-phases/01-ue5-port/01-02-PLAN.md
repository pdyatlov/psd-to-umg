---
phase: 01-ue5-port
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - Content/Python/auto_psd_ui.py
  - Content/Python/AutoPSDUI/__init__.py
  - Content/Python/AutoPSDUI/common.py
  - Content/Python/AutoPSDUI/psd_utils.py
  - Source/ThirdParty/Mac/
autonomous: false
requirements:
  - PORT-03
  - PORT-04

must_haves:
  truths:
    - "Python scripts preserved as reference in docs/python-reference/"
    - "Mac ThirdParty directory deleted"
    - "Content/Python/ directory no longer exists"
    - "Plugin compiles in UE 5.7.4 with zero errors"
    - "Plugin loads in UE 5.7.4 editor without errors or warnings"
    - "Plugin appears in Edit > Plugins as PSD2UMG"
  artifacts:
    - path: "docs/python-reference/auto_psd_ui.py"
      provides: "Archived Python orchestration script"
    - path: "docs/python-reference/AutoPSDUI/common.py"
      provides: "Archived Python common utilities"
    - path: "docs/python-reference/AutoPSDUI/psd_utils.py"
      provides: "Archived Python PSD parsing utilities"
  key_links:
    - from: "AutoPSDUI.uplugin"
      to: "Source/AutoPSDUI/AutoPSDUI.Build.cs"
      via: "Module name PSD2UMG must match in both files"
      pattern: "PSD2UMG"
---

<objective>
Clean up legacy Python scripts and ThirdParty dependencies, then verify the renamed plugin compiles and loads in UE 5.7.4.

Purpose: Remove dead code (Python pipeline, Mac third-party Python packages) while preserving scripts as reference for future C++ reimplementation. Then verify the complete port works end-to-end.
Output: Clean plugin directory with no Python runtime dependency. Verified compilation and editor load.
</objective>

<execution_context>
@C:/Users/pavel.dyatlov/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pavel.dyatlov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ue5-port/01-CONTEXT.md
@.planning/phases/01-ue5-port/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Python scripts to docs/ and delete ThirdParty/Mac</name>
  <files>Content/Python/, Source/ThirdParty/Mac/, docs/python-reference/</files>
  <read_first>.planning/phases/01-ue5-port/01-CONTEXT.md</read_first>
  <action>
Per D-18 and D-19, perform the following file operations:

1. **Move Content/Python/ to docs/python-reference/** (D-18):
   ```bash
   mkdir -p docs/python-reference
   # Move (not copy) the entire Content/Python/ tree
   mv Content/Python/auto_psd_ui.py docs/python-reference/
   mv Content/Python/AutoPSDUI/ docs/python-reference/AutoPSDUI/
   # Remove the now-empty Content/Python/ directory
   rmdir Content/Python/
   # If Content/ is now empty, remove it too
   rmdir Content/ 2>/dev/null || true
   ```

2. **Delete Source/ThirdParty/Mac/ entirely** (D-19):
   ```bash
   rm -rf Source/ThirdParty/Mac/
   ```

3. **Check if Source/ThirdParty/Win64/ exists and has content**:
   - If it contains Python packages similar to Mac (PIL, numpy, etc.), also delete it: `rm -rf Source/ThirdParty/Win64/`
   - If Source/ThirdParty/ is now empty, remove it: `rmdir Source/ThirdParty/`

4. **Verify no Content/Python/ or Source/ThirdParty/Mac/ remains**.
  </action>
  <verify>
    <automated>cd "C:/Dev/psd-to-umg" && echo "=== docs/python-reference exists ===" && ls docs/python-reference/auto_psd_ui.py && ls docs/python-reference/AutoPSDUI/common.py && echo "=== Content/Python gone ===" && test ! -d Content/Python && echo "PASS: Content/Python removed" || echo "FAIL: Content/Python still exists" && echo "=== ThirdParty/Mac gone ===" && test ! -d Source/ThirdParty/Mac && echo "PASS: ThirdParty/Mac removed" || echo "FAIL: ThirdParty/Mac still exists"</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/python-reference/auto_psd_ui.py` succeeds (file exists)
    - `ls docs/python-reference/AutoPSDUI/common.py` succeeds (file exists)
    - `ls docs/python-reference/AutoPSDUI/psd_utils.py` succeeds (file exists)
    - `test -d Content/Python` fails (directory removed)
    - `test -d Source/ThirdParty/Mac` fails (directory removed)
    - Python scripts are preserved as reference, not deleted
  </acceptance_criteria>
  <done>
    Python scripts moved to docs/python-reference/ for reference. Mac ThirdParty deleted. No Python runtime files in active plugin directory.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify plugin compiles and loads in UE 5.7.4</name>
  <files>AutoPSDUI.uplugin</files>
  <action>
This is a human verification checkpoint. The executor should present the verification steps below to the user.
All automated work is complete from Plan 01 and Task 1 of this plan.
The user needs to manually compile and test the plugin in UE 5.7.4 per D-20 and D-21.
  </action>
  <what-built>
    The complete Phase 1 port: all C++ classes renamed from AutoPSDUI to PSD2UMG, .uplugin updated for UE 5.7, Build.cs cleaned of Python dependencies, deprecated APIs replaced (AssetRegistry include path, IPythonScriptPlugin removed), ShutdownModule delegate cleanup added, GEditor null guard added, RunPyCmd stubbed, Python scripts archived to docs/, ThirdParty/Mac deleted.
  </what-built>
  <how-to-verify>
    **Per D-20 and D-21, follow these exact steps:**

    1. Copy the plugin into a UE 5.7.4 test project:
       - Create or use an existing UE 5.7.4 C++ project
       - Copy the entire `psd-to-umg` folder into the project's `Plugins/PSD2UMG/` directory
       - NOTE: The source folder is still named `Source/AutoPSDUI/` (per D-01, directory rename deferred), but the module Name in .uplugin and Build.cs is `PSD2UMG`

    2. Generate project files:
       - Right-click the .uproject file > "Generate Visual Studio project files"
       - Or run: `"C:/Program Files/Epic Games/UE_5.7/Engine/Build/BatchFiles/Build.bat" YourProject Win64 Development -Project="path/to/YourProject.uproject"`

    3. Compile:
       - Open in Visual Studio/Rider and build
       - Or use `Build.bat` from command line
       - **Expected: Zero errors, zero deprecated-API warnings** (D-21)

    4. Open UE 5.7.4 editor with the project

    5. Check Output Log:
       - **Expected: No errors or warnings related to PSD2UMG** (PORT-03)
       - Search for "PSD2UMG" in Output Log -- should see module load message, nothing else

    6. Check Edit > Plugins:
       - **Expected: "PSD2UMG" appears under Editor category** (PORT-04)
       - Description should read: "Import PSD files as UMG Widget Blueprints"

    7. Check Project Settings > Plugins:
       - **Expected: "PSD2UMG" section appears** with bEnabled checkbox, TextureSrcDir, TextureAssetDir, FontMap, DefaultFont fields

    8. Quick functional test:
       - Drop any .psd file into Content Browser
       - Check Output Log for: "PSD2UMG: PSD file detected (...) but native import pipeline not yet implemented"
       - This confirms the import hook is working correctly

    Report any compilation errors, warnings, or unexpected behavior.
  </how-to-verify>
  <verify>
    <automated>MISSING -- Requires UE 5.7.4 project compilation which cannot be automated from CLI without a pre-configured project. Human must verify manually per steps above.</automated>
  </verify>
  <acceptance_criteria>
    - UE 5.7.4 compilation produces zero errors
    - UE 5.7.4 compilation produces zero deprecated-API warnings
    - Editor Output Log shows no PSD2UMG errors on startup
    - Edit > Plugins shows "PSD2UMG" with description "Import PSD files as UMG Widget Blueprints"
    - Project Settings > Plugins > PSD2UMG section is visible with all settings fields
    - Dropping a .psd file produces log: "PSD2UMG: PSD file detected ... but native import pipeline not yet implemented"
  </acceptance_criteria>
  <done>
    Plugin compiles and loads cleanly in UE 5.7.4. All PORT-01 through PORT-04 requirements verified by human.
  </done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. Plugin compiles with zero errors and zero deprecated-API warnings in UE 5.7.4
2. Plugin loads without errors in the editor Output Log
3. Plugin appears as "PSD2UMG" in Edit > Plugins
4. PSD2UMG settings section appears in Project Settings > Plugins
5. Dropping a .psd file produces the expected deprecation log message
6. No Python files remain in the active plugin directory (Content/Python/ gone)
7. No Mac ThirdParty packages remain (Source/ThirdParty/Mac/ gone)
</verification>

<success_criteria>
- Zero compile errors in UE 5.7.4
- Zero deprecated-API warnings
- Plugin loads cleanly in editor
- Plugin visible as "PSD2UMG" in Plugins browser
- Settings accessible in Project Settings
- Import hook fires correctly (log message on .psd import)
- Python reference scripts preserved in docs/
</success_criteria>

<output>
After completion, create `.planning/phases/01-ue5-port/01-02-SUMMARY.md`
</output>

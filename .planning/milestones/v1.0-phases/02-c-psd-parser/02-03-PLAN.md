---
phase: 02-c-psd-parser
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - Source/PSD2UMG/Private/Parser/PsdParser.cpp
  - Source/PSD2UMG/Public/Parser/PsdParser.h
  - Source/PSD2UMG/Private/Parser/PsdParserInternal.h
autonomous: true
requirements: [PRSR-02, PRSR-03, PRSR-05]
must_haves:
  truths:
    - "PSD2UMG::Parser::ParseFile(Path, OutDoc, OutDiag) returns true on a valid PSD"
    - "OutDoc.CanvasSize matches the PSD header width/height"
    - "OutDoc.RootLayers preserves PhotoshopAPI's parent-child group hierarchy"
    - "Each FPsdLayer has Name, Type, Bounds, Opacity (0..1), bVisible populated"
    - "Image layers have RGBAPixels populated with length == PixelWidth * PixelHeight * 4"
    - "Exceptions from PhotoshopAPI are caught and converted into FPsdDiagnostic::Error entries"
  artifacts:
    - path: "Source/PSD2UMG/Public/Parser/PsdParser.h"
      provides: "namespace PSD2UMG::Parser { bool ParseFile(const FString&, FPsdDocument&, FPsdParseDiagnostics&); }"
      exports: ["ParseFile"]
    - path: "Source/PSD2UMG/Private/Parser/PsdParser.cpp"
      provides: "Implementation calling PhotoshopAPI, PIMPL-insulated"
      min_lines: 150
  key_links:
    - from: "Source/PSD2UMG/Private/Parser/PsdParser.cpp"
      to: "Source/ThirdParty/PhotoshopAPI/Win64/include"
      via: "#include <PhotoshopAPI.h>"
      pattern: "PhotoshopAPI"
    - from: "Source/PSD2UMG/Private/Parser/PsdParser.cpp"
      to: "Source/PSD2UMG/Public/Parser/PsdTypes.h"
      via: "populates FPsdDocument"
      pattern: "FPsdDocument"
---

<objective>
Implement `PSD2UMG::Parser::ParseFile` — the core native PSD reader. Opens a .psd via PhotoshopAPI, walks the layer tree, and populates `FPsdDocument` with layer names, types, bounds, opacity, visibility, group hierarchy, and image RGBA pixel data. Text layer content handled in this plan as well (single-run extraction per CONTEXT.md D-Text).

Purpose: The actual parser — PRSR-02, PRSR-03, PRSR-04, PRSR-05 land here.
Output: A working `ParseFile` behind a PIMPL wall (no PhotoshopAPI types in public headers).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-c-psd-parser/02-CONTEXT.md
@.planning/phases/02-c-psd-parser/02-RESEARCH.md
@.planning/phases/02-c-psd-parser/02-02-SUMMARY.md
@Source/PSD2UMG/Public/Parser/PsdTypes.h
@Source/PSD2UMG/Public/Parser/PsdDiagnostics.h
@Source/PSD2UMG/Public/PSD2UMGLog.h
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Public ParseFile header + PIMPL internal header</name>
  <files>Source/PSD2UMG/Public/Parser/PsdParser.h, Source/PSD2UMG/Private/Parser/PsdParserInternal.h</files>
  <action>
    `Public/Parser/PsdParser.h`:
    ```cpp
    #pragma once
    #include "CoreMinimal.h"
    #include "Parser/PsdTypes.h"
    #include "Parser/PsdDiagnostics.h"
    namespace PSD2UMG::Parser
    {
        PSD2UMG_API bool ParseFile(const FString& Path, FPsdDocument& OutDoc, FPsdParseDiagnostics& OutDiag);
    }
    ```
    Per CONTEXT.md D-Linkage: free function in namespace `PSD2UMG::Parser`, stateless. No PhotoshopAPI types in this header.

    `Private/Parser/PsdParserInternal.h`: small helper header for the .cpp with forward declarations and internal helpers (e.g., `bool ConvertLayerRecursive(const psapi::Layer&, FPsdLayer&, FPsdParseDiagnostics&)`). This file CAN include PhotoshopAPI headers since it's private.
  </action>
  <verify>
    <automated>grep -q "namespace PSD2UMG::Parser" Source/PSD2UMG/Public/Parser/PsdParser.h &amp;&amp; ! grep -q "PhotoshopAPI" Source/PSD2UMG/Public/Parser/PsdParser.h</automated>
  </verify>
  <done>Public header exists, PhotoshopAPI-free; internal header scaffolded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement ParseFile — document load, group hierarchy, bounds/opacity/visibility, image RGBA, single-run text</name>
  <files>Source/PSD2UMG/Private/Parser/PsdParser.cpp</files>
  <action>
    Implement `PSD2UMG::Parser::ParseFile` per CONTEXT.md. Structure:

    1. Wrap entire body in `try { ... } catch (const std::exception& e) { OutDiag.AddError(TEXT(""), FString(ANSI_TO_TCHAR(e.what()))); return false; } catch (...) { OutDiag.AddError(...); return false; }` — per CONTEXT.md D-Linkage, exceptions must never escape.

    2. Use PhotoshopAPI's document loading API (consult 02-RESEARCH.md and upstream README for exact calls). Pseudo:
       ```cpp
       NAMESPACE_PSAPI::LayeredFile<uint8_t> File = NAMESPACE_PSAPI::LayeredFile<uint8_t>::read(TCHAR_TO_UTF8(*Path));
       OutDoc.SourcePath = Path;
       OutDoc.CanvasSize = FIntPoint(File.width(), File.height());
       ```
       (Exact API surface: verify from vendored headers in 02-01. Use `uint8_t` bit depth for v1 — matches CONTEXT.md's 8-bit RGBA assumption.)

    3. Recursive walker `ConvertLayerRecursive(const Layer&, FPsdLayer& Out, FPsdParseDiagnostics&)`:
       - Populate `Name`, `Bounds` (`FIntRect(left, top, right, bottom)`), `Opacity` (PhotoshopAPI 0-255 → 0.f..1.f), `bVisible`.
       - Use `std::dynamic_pointer_cast` to distinguish `GroupLayer`, `ImageLayer`, `TextLayer`. This is why Build.cs sets `bUseRTTI = true`.
       - Group → `Type = EPsdLayerType::Group`; recurse into children, append to `Out.Children`.
       - Image → `Type = EPsdLayerType::Image`; extract R/G/B/A channels from `ImageLayer`, interleave into `RGBAPixels`, set `PixelWidth`/`PixelHeight` from layer bounds. Handle missing alpha (default 255).
       - Text → `Type = EPsdLayerType::Text`; call helper `ExtractSingleRunText(TextLayer, Out.Text, OutDiag)`:
         - Read content via PhotoshopAPI text API
         - Read font family name (first run)
         - Read size in pt and assign directly: `SizePx = static_cast<float>(Pt);` — at 72 DPI, 1 pt == 1 px (standard PSD assumption). Do NOT apply the `× 0.75` UMG-DPI conversion here; that belongs to Phase 4 (TEXT-01) which owns the full text pipeline.
         - Read color (sRGB byte triple) → `FLinearColor::FromSRGBColor(FColor(r,g,b))`
         - Read alignment → map to `ETextJustify::Left/Center/Right`
         - If multiple runs exist: concatenate content, take first run's style, emit `OutDiag.AddWarning(LayerName, TEXT("Multi-run text flattened to first run's style. Multi-run support arrives in Phase 4."))` per CONTEXT.md.
       - Unknown layer kind → `Type = EPsdLayerType::Unknown`; emit diagnostic warning.

    4. Walk `File.layers()` top-level, populate `OutDoc.RootLayers`.

    5. Return `true` if no errors in diagnostics, else `false`. Log one summary line via `UE_LOG(LogPSD2UMG, Log, ...)` with counts.

    Notes:
    - Include `<PhotoshopAPI.h>` (or the actual top-level header name discovered during 02-01 vendoring) — Private/ only.
    - Wrap `#include`s of PhotoshopAPI in `THIRD_PARTY_INCLUDES_START` / `THIRD_PARTY_INCLUDES_END` per UE convention to silence warnings.
    - Use `TCHAR_TO_UTF8` for path conversion.
    - If the exact PhotoshopAPI API shape differs from the pseudo above, consult vendored headers and adapt — the contract is the `FPsdDocument` output, not the internal calls.
  </action>
  <verify>
    <automated>grep -q "namespace PSD2UMG::Parser" Source/PSD2UMG/Private/Parser/PsdParser.cpp &amp;&amp; grep -q "try" Source/PSD2UMG/Private/Parser/PsdParser.cpp &amp;&amp; grep -q "THIRD_PARTY_INCLUDES_START" Source/PSD2UMG/Private/Parser/PsdParser.cpp &amp;&amp; grep -q "EPsdLayerType::Group" Source/PSD2UMG/Private/Parser/PsdParser.cpp &amp;&amp; grep -q "EPsdLayerType::Image" Source/PSD2UMG/Private/Parser/PsdParser.cpp &amp;&amp; grep -q "EPsdLayerType::Text" Source/PSD2UMG/Private/Parser/PsdParser.cpp</automated>
  </verify>
  <done>
    Parser compiles, handles all four layer types, catches exceptions, populates FPsdDocument fully.
    End-to-end correctness verified in plan 02-05 by the automation spec against the fixture PSD.
  </done>
</task>

</tasks>

<verification>
- Module compiles successfully in Development Editor, Win64
- `PsdParser.cpp` includes PhotoshopAPI headers (inside THIRD_PARTY_INCLUDES block)
- Public header does NOT include PhotoshopAPI
</verification>

<success_criteria>
- ParseFile implementation exists and compiles
- Functional correctness gate is plan 02-05's Automation Spec
</success_criteria>

<output>
Create `.planning/phases/02-c-psd-parser/02-03-SUMMARY.md` documenting the exact PhotoshopAPI API surface used, any deviations from CONTEXT.md, and the pt→px conversion math chosen.
</output>

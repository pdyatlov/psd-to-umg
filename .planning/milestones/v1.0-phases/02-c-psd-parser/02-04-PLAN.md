---
phase: 02-c-psd-parser
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - Source/PSD2UMG/Public/Factories/PsdImportFactory.h
  - Source/PSD2UMG/Private/Factories/PsdImportFactory.cpp
  - Source/PSD2UMG/Private/PSD2UMG.cpp
files_modified_notes: "PSD2UMG.cpp is modified to REMOVE the OnAssetReimport delegate hook (replaced by factory)."
autonomous: true
requirements: [PRSR-06, PRSR-07]
must_haves:
  truths:
    - "UPsdImportFactory is registered for .psd files with higher ImportPriority than UTextureFactory"
    - "Dropping a .psd into the Content Browser routes through UPsdImportFactory (not UTextureFactory)"
    - "The factory internally forwards to UTextureFactory and preserves the resulting UTexture2D"
    - "The factory calls PSD2UMG::Parser::ParseFile on the same bytes and logs a summary line via LogPSD2UMG"
    - "FPSD2UMGModule no longer subscribes to ImportSubsystem->OnAssetReimport"
  artifacts:
    - path: "Source/PSD2UMG/Public/Factories/PsdImportFactory.h"
      provides: "UPsdImportFactory UCLASS deriving from UFactory"
    - path: "Source/PSD2UMG/Private/Factories/PsdImportFactory.cpp"
      provides: "FactoryCreateBinary override: wraps UTextureFactory + runs FPsdParser"
      min_lines: 80
  key_links:
    - from: "Source/PSD2UMG/Private/Factories/PsdImportFactory.cpp"
      to: "Source/PSD2UMG/Public/Parser/PsdParser.h"
      via: "PSD2UMG::Parser::ParseFile"
      pattern: "ParseFile"
    - from: "Source/PSD2UMG/Private/Factories/PsdImportFactory.cpp"
      to: "UTextureFactory (engine)"
      via: "NewObject<UTextureFactory>() + FactoryCreateBinary"
      pattern: "UTextureFactory"
---

<objective>
Register `UPsdImportFactory` as the .psd importer in wrapper mode per CONTEXT.md D-Factory: it claims .psd with higher priority than `UTextureFactory`, internally forwards to `UTextureFactory` to preserve the flat UTexture2D, then runs `PSD2UMG::Parser::ParseFile` on the same bytes. Remove the Phase 1 `OnAssetReimport` delegate hook — this factory replaces it.

Purpose: Close PRSR-06 and firmly end Python's role (PRSR-07) by owning the .psd import entry point.
Output: Dropping a .psd in Content Browser triggers parser and logs a layer-tree summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-c-psd-parser/02-CONTEXT.md
@.planning/phases/02-c-psd-parser/02-03-SUMMARY.md
@Source/PSD2UMG/Public/Parser/PsdParser.h
@Source/PSD2UMG/Private/PSD2UMG.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: UPsdImportFactory UCLASS + FactoryCreateBinary wrapper</name>
  <files>Source/PSD2UMG/Public/Factories/PsdImportFactory.h, Source/PSD2UMG/Private/Factories/PsdImportFactory.cpp</files>
  <action>
    Create `UPsdImportFactory : public UFactory`:

    Header (`Public/Factories/PsdImportFactory.h`):
    - `UCLASS()` with `GENERATED_BODY()`
    - Constructor: set `bEditorImport = true`, `bText = false`, add `"psd"` to `Formats` (as `"psd;Photoshop Document"`), set `SupportedClass = UTexture2D::StaticClass()` (matches UTextureFactory's claim so we can win priority), set `ImportPriority = DefaultImportPriority + 100` per CONTEXT.md D-Factory.
    - Override `FactoryCreateBinary`.

    Implementation (`Private/Factories/PsdImportFactory.cpp`):
    1. `FactoryCreateBinary(UClass* InClass, UObject* InParent, FName InName, EObjectFlags Flags, UObject* Context, const TCHAR* Type, const uint8*& Buffer, const uint8* BufferEnd, FFeedbackContext* Warn)`:
       - Instantiate inner `UTextureFactory* Inner = NewObject<UTextureFactory>(GetTransientPackage(), UTextureFactory::StaticClass());`
       - `Inner->AddToRoot();` / scope guard to remove
       - Forward: `UObject* Result = Inner->FactoryCreateBinary(UTexture2D::StaticClass(), InParent, InName, Flags, Context, Type, Buffer, BufferEnd, Warn);` — this preserves the flat UTexture2D behavior exactly as before (CONTEXT.md D-Factory: "The flat texture is preserved as a backwards-compatible side effect").
       - Write the raw `Buffer..BufferEnd` bytes to a temp file (`FPaths::CreateTempFilename(*FPaths::ProjectIntermediateDir(), TEXT("PSD2UMG_"), TEXT(".psd"))`) so the parser can ingest by path. PhotoshopAPI's `LayeredFile::read` takes a path.
       - Call `FPsdDocument Doc; FPsdParseDiagnostics Diag; bool bOk = PSD2UMG::Parser::ParseFile(TempPath, Doc, Diag);`
       - Delete the temp file.
       - `UE_LOG(LogPSD2UMG, Log, TEXT("PSD2UMG: parsed %s — %d root layers, %d warnings, %d errors"), *InName.ToString(), Doc.RootLayers.Num(), WarningCount, ErrorCount);` — CONTEXT.md D-Logging: Output Log summary line per import.
       - Return `Result` (the UTexture2D). Phase 3 will extend this to also produce a WBP from `Doc`.

    2. Handle null/empty buffer defensively: if bytes empty, skip parser, return Inner's result.

    Notes:
    - Include `Factories/TextureFactory.h` for UTextureFactory.
    - Include `Misc/Paths.h`, `Misc/FileHelper.h` for temp file IO.
    - `LOCTEXT_NAMESPACE "UPsdImportFactory"` for any text literals.
  </action>
  <verify>
    <automated>grep -q "class UPsdImportFactory" Source/PSD2UMG/Public/Factories/PsdImportFactory.h &amp;&amp; grep -q "UTextureFactory" Source/PSD2UMG/Private/Factories/PsdImportFactory.cpp &amp;&amp; grep -q "PSD2UMG::Parser::ParseFile" Source/PSD2UMG/Private/Factories/PsdImportFactory.cpp &amp;&amp; grep -q "ImportPriority" Source/PSD2UMG/Private/Factories/PsdImportFactory.cpp</automated>
  </verify>
  <done>Factory class exists with wrapper-mode FactoryCreateBinary; registers itself via UFactory metadata; compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove OnAssetReimport delegate from FPSD2UMGModule (factory replaces it)</name>
  <files>Source/PSD2UMG/Private/PSD2UMG.cpp</files>
  <action>
    Per CONTEXT.md D-Factory and `<specifics>`: the factory replaces the Phase 1 delegate hook.

    Edit `Source/PSD2UMG/Private/PSD2UMG.cpp`:
    - `StartupModule()`: remove the `ImportSubsystem->OnAssetReimport.AddRaw(...)` block. Leave an empty body (or a one-line log `UE_LOG(LogPSD2UMG, Log, TEXT("PSD2UMG module loaded"));`).
    - `ShutdownModule()`: remove the matching `RemoveAll` block. Leave empty body.
    - Remove `OnPSDImport` method from both header and cpp (it was the legacy hook). Also clean the header `PSD2UMG.h` declaration.
    - Remove unused includes: `Editor.h`, `Subsystems/ImportSubsystem.h`, `EditorFramework/AssetImportData.h`, `PSD2UMGSetting.h` (if only referenced for bEnabled check inside OnPSDImport — keep if still used elsewhere), `PSD2UMGLibrary.h` (unless needed).
    - Do NOT touch `UPSD2UMGSettings` itself — just remove this file's dependency if OnPSDImport was the only consumer.
    - **DO NOT delete `PSD2UMGLibrary.{h,cpp}` or remove the `RunPyCmd` stub.** Per CONTEXT.md `<specifics>`, the deprecated `RunPyCmd` stub is preserved through Phase 2 and only cleaned up in Phase 6+. Leave the library file untouched; only remove the `#include "PSD2UMGLibrary.h"` line from `PSD2UMG.cpp` if nothing in this file references it anymore.

    Add `#include "PSD2UMGLog.h"` if using the LogPSD2UMG category.
  </action>
  <verify>
    <automated>! grep -q "OnAssetReimport" Source/PSD2UMG/Private/PSD2UMG.cpp &amp;&amp; ! grep -q "OnPSDImport" Source/PSD2UMG/Private/PSD2UMG.cpp</automated>
  </verify>
  <done>Module startup/shutdown no longer subscribes to OnAssetReimport. Compiles cleanly. Factory is the sole .psd entry point.</done>
</task>

</tasks>

<verification>
- Module compiles
- Factory is registered (verified at runtime in plan 02-05 via Automation Spec and manual drop)
</verification>

<success_criteria>
- PRSR-06 satisfied: UPsdImportFactory owns .psd imports
- PRSR-07 satisfied: no Python anywhere in the .psd code path
- Phase 1 delegate-hook deviation cleaned up
</success_criteria>

<output>
Create `.planning/phases/02-c-psd-parser/02-04-SUMMARY.md` noting the chosen ImportPriority delta, any subtleties around temp-file IO vs in-memory parser input, and the exact list of includes removed from PSD2UMG.cpp.
</output>

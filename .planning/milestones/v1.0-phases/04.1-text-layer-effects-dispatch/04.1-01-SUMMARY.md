---
phase: 04.1-text-layer-effects-dispatch
plan: "01"
subsystem: parser, mapper, tests
tags: [text, shadow, TEXT-04, effects-routing]
dependency_graph:
  requires: []
  provides: [FPsdTextRun.ShadowOffset, FPsdTextRun.ShadowColor, RouteTextEffects, FTextEffectsSpec]
  affects: [FTextLayerMapper, PsdParser, PsdTypes]
tech_stack:
  added: []
  patterns: [D-09 Option B DPI conversion at mapper, D-13 double-render guard via flag clear]
key_files:
  created:
    - Source/PSD2UMG/Tests/FTextEffectsSpec.cpp
    - Source/PSD2UMG/Tests/Fixtures/TextEffects.psd
  modified:
    - Source/PSD2UMG/Public/Parser/PsdTypes.h
    - Source/PSD2UMG/Private/Parser/PsdParser.cpp
    - Source/PSD2UMG/Private/Mapper/FTextLayerMapper.cpp
decisions:
  - DPI conversion (x0.75) applied in FTextLayerMapper (D-09 Option B) matching existing OutlineSize pattern
  - Shadow fields added as plain (non-UPROPERTY) members on FPsdTextRun -- safe per UE reflection rules
  - RouteTextEffects placed in Internal namespace before ConvertLayerRecursive for easy Plan 02 extension
  - FTextEffectsSpec gates all assertions on FPaths::FileExists(FixturePath) -- deferred fixture is not a test failure
metrics:
  duration: ~15m
  completed_date: "2026-04-15"
  tasks_completed: 2 of 2
  files_changed: 5
---

# Phase 4.1 Plan 01: Text Effects Dispatch (Shadow) Summary

**One-liner:** TEXT-04 gap closed by routing Photoshop Drop Shadow from `FPsdLayerEffects` into `FPsdTextRun` via `RouteTextEffects`, with `FTextLayerMapper` applying `SetShadowOffset`/`SetShadowColorAndOpacity` at 0.75x DPI scale; spec skeleton awaits `TextEffects.psd` fixture.

## What Was Built

### Task 1 -- FPsdTextRun shadow fields + RouteTextEffects helper

**File:** `Source/PSD2UMG/Public/Parser/PsdTypes.h`
- Replaced the `// TEXT-04 fields intentionally omitted` comment block (lines 52-58) with two active field declarations:
  ```cpp
  FVector2D ShadowOffset = FVector2D::ZeroVector;
  FLinearColor ShadowColor = FLinearColor::Transparent;
  ```
- Unit comment documents raw PSD pixel units and D-09 Option B DPI convention.

**File:** `Source/PSD2UMG/Private/Parser/PsdParser.cpp`
- Added `static void RouteTextEffects(FPsdLayer& Layer)` in `namespace PSD2UMG::Parser::Internal` immediately before `ConvertLayerRecursive`. The helper:
  - Guards on `Layer.Type != EPsdLayerType::Text` (no-op for image/group).
  - Copies `Effects.DropShadowOffset` -> `Text.ShadowOffset` and `Effects.DropShadowColor` -> `Text.ShadowColor`.
  - Clears `Effects.bHasDropShadow = false` (D-13 double-render guard — prevents generator's image-shadow path from spawning a sibling `_Shadow` UImage).
  - Comment stub for Plan 04.1-02 stroke routing (minimal diff for `bHasStroke` extension).
- Added `RouteTextEffects(OutLayer);` call in the TextLayer branch of `ConvertLayerRecursive`, between `ExtractSingleRunText` and `return`.

**Commit:** `a682eae`

### Task 2 -- FTextLayerMapper shadow apply + FTextEffectsSpec

**File:** `Source/PSD2UMG/Private/Mapper/FTextLayerMapper.cpp`
- Deleted the 7-line deferred `// TEXT-04 -- drop shadow DEFERRED.` comment block (lines 106-112).
- Replaced with active shadow apply block immediately after the outline block (before the TEXT-06 AutoWrapText call):
  ```cpp
  if (!Layer.Text.ShadowOffset.IsZero() || Layer.Text.ShadowColor.A > 0.f)
  {
      TextWidget->SetShadowOffset(Layer.Text.ShadowOffset * 0.75);
      TextWidget->SetShadowColorAndOpacity(Layer.Text.ShadowColor);
  }
  ```
- DPI conversion (x0.75) at mapper call site — D-09 Option B.

**File:** `Source/PSD2UMG/Tests/FTextEffectsSpec.cpp` (new)
- Automation spec `PSD2UMG.TextEffects` with 7 test cases:
  1. `text_shadow` layer has non-zero `Text.ShadowOffset` after parsing
  2. `text_shadow` layer has `Effects.bHasDropShadow == false` after routing
  3. `text_shadow` UTextBlock has DPI-converted shadow offset (golden: -5.3, +5.3 ±0.2)
  4. `text_shadow` UTextBlock has shadow color A ≈ 0.5 (±0.05)
  5. `text_plain` UTextBlock has zero shadow offset
  6. `text_plain` UTextBlock has transparent shadow color (A < 0.01)
  7. No `_Shadow` UImage sibling spawned for shadow text layers (D-13 guard)
- All tests gated on `FPaths::FileExists(FixturePath)` -- silently skip if `TextEffects.psd` not yet authored. This is intentional so the spec compiles and runs in CI before the fixture PSD exists (fixture is a human-action checkpoint).
- `BeforeEach`: resolves fixture path via `IPluginManager`, parses, generates WBP into transient package.
- `AfterEach`: force-deletes the transient WBP asset to avoid inter-test pollution.

**Commit:** `5a756ac`

## DPI Conversion Site

**Choice:** D-09 Option B -- DPI conversion at mapper call site (`FTextLayerMapper.cpp`).

**Rationale:**
- Matches the existing `OutlineSize * 0.75f` pattern at line 97 of the same file.
- Keeps `FPsdTextRun` field units uniform (all "raw PSD pixels") -- parser and spec authors read a consistent unit across all text fields.
- Mapper is the established DPI boundary per TEXT-01 (font size conversion ships Phase 3).

## Fixture Status

**COMMITTED** (commit `ca189a7`).

`Source/PSD2UMG/Tests/Fixtures/TextEffects.psd` authored in Photoshop and committed:
- Layer `text_shadow`: text "Shadow", Drop Shadow Distance=10px, Angle=135°, Color=#000000, Opacity=50%, Spread=0, Size=0.
- Layer `text_plain`: text "Plain", no layer effects.
- Additional layers for Plan 04.1-02: `text_stroke`, `text_stroke_and_shadow`, `text_complex` (harmless for Plan 01 tests; FTextEffectsSpec only looks up `text_shadow` and `text_plain` by name).

`PSD2UMG.TextEffects` spec will run against this fixture. Build/run verification deferred to CI when branch is consumed by host project (plugin-only repo -- no `.uproject`, see Deviations section).

Plan 04.1-02 will extend `TextEffects.psd` test cases with stroke/combined/complex layers.

## TEXT-04 Acceptance Criteria Status

| Criterion | Status |
|-----------|--------|
| `FPsdTextRun` has `ShadowOffset` + `ShadowColor` fields | DONE |
| `RouteTextEffects` helper exists in parser, called from TextLayer branch only | DONE |
| `FTextLayerMapper` applies `SetShadowOffset`/`SetShadowColorAndOpacity` | DONE |
| Deferred comment blocks removed from both PsdTypes.h and FTextLayerMapper.cpp | DONE |
| `TextEffects.psd` fixture with `text_shadow` + `text_plain` | DONE (commit `ca189a7`) |
| `PSD2UMG.TextEffects` spec green | PENDING (requires host project for CI run) |
| TEXT-04 removable from v1.0 gap list | PENDING (spec must run green in CI) |

## Residual Gaps for Plan 02

- TEXT-03 (Stroke): `FPsdLayerEffects` needs `bHasStroke / StrokeColor / StrokeSize`. `lfx2` descriptor walker (~80 lines) needed in `ExtractLayerEffects`.
- `RouteTextEffects` needs `bHasStroke` branch (extend existing helper, minimal diff).
- `PsdParserSpec.cpp:266-275` `text_stroked` assertion inversion: currently asserts `bHasComplexEffects == true` (correct until Plan 02 decodes `lfx2`); Plan 02 inverts this.
- `TextEffects.psd` stroke layers: `text_stroke`, `text_stroke_and_shadow`, `text_complex` (also human-action but for Plan 02).

## Deviations from Plan

### Out-of-scope: No host project -- build verification skipped

- **Found during:** Task 1 verification step.
- **Issue:** This repo is plugin-only -- no `*.uproject` exists. The plan's `verify` command (`Build.bat ... -Project=PSD2UMG.uproject`) cannot run.
- **Resolution:** Same outcome as Phase 9 Plans 01-04 (documented in their SUMMARYs). Implementation follows canonical UE 5.7 + C++20 idioms used throughout codebase. Build/test verification deferred to CI when branch is consumed by host project (post-v1 per Phase 8 D-06).
- **Tracking:** Known limitation, not a behavioral deviation.

## Self-Check: PASSED

- Source/PSD2UMG/Public/Parser/PsdTypes.h -- modified, ShadowOffset + ShadowColor present
- Source/PSD2UMG/Private/Parser/PsdParser.cpp -- modified, RouteTextEffects present, called from TextLayer branch
- Source/PSD2UMG/Private/Mapper/FTextLayerMapper.cpp -- modified, SetShadowOffset call present
- Source/PSD2UMG/Tests/FTextEffectsSpec.cpp -- created
- Commits a682eae, 5a756ac -- present in git log

## Known Stubs

None. The `FTextEffectsSpec.cpp` is fully wired to the real code path; the fixture-guard (`FPaths::FileExists`) is intentional checkpoint behavior, not a stub.

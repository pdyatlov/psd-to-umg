---
phase: 07-editor-ui-preview-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/PSD2UMG/Public/PSD2UMGSetting.h
  - Source/PSD2UMG/Private/PSD2UMGSetting.cpp
  - Source/PSD2UMG/PSD2UMG.Build.cs
autonomous: true
requirements: [EDITOR-01]
must_haves:
  truths:
    - "Plugin settings appear in Project Settings > Plugins > PSD2UMG with grouped categories"
    - "New settings bShowPreviewDialog, bUseCommonUI, InputActionSearchPath, SourceDPI exist with correct defaults"
    - "Build.cs includes ToolMenus, ContentBrowser, MovieScene, MovieSceneTracks, EnhancedInput, CommonUI modules"
  artifacts:
    - path: "Source/PSD2UMG/Public/PSD2UMGSetting.h"
      provides: "Extended settings class with 4 new properties and category grouping"
      contains: "bShowPreviewDialog"
    - path: "Source/PSD2UMG/PSD2UMG.Build.cs"
      provides: "Module dependencies for phase 7 features"
      contains: "ToolMenus"
  key_links:
    - from: "Source/PSD2UMG/Public/PSD2UMGSetting.h"
      to: "UDeveloperSettings"
      via: "Category pipe-separated grouping"
      pattern: "PSD2UMG\\|"
---

<objective>
Add new plugin settings (bShowPreviewDialog, bUseCommonUI, InputActionSearchPath, SourceDPI) with category grouping and add all Build.cs module dependencies needed for Phase 7.

Purpose: Foundation for all Phase 7 features — settings control behavior, modules enable compilation.
Output: Extended UPSD2UMGSettings, updated Build.cs
</objective>

<execution_context>
@C:\Users\pavel.dyatlov\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pavel.dyatlov\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-editor-ui-preview-settings/07-CONTEXT.md
@.planning/phases/07-editor-ui-preview-settings/07-RESEARCH.md

<interfaces>
From Source/PSD2UMG/Public/PSD2UMGSetting.h:
```cpp
UCLASS(config = Editor, defaultconfig, meta=(DisplayName="PSD2UMG"))
class PSD2UMG_API UPSD2UMGSettings : public UDeveloperSettings
{
    // Existing: bEnabled, bFlattenComplexEffects, MaxSmartObjectDepth,
    // TextureSrcDir, TextureAssetDir, WidgetBlueprintAssetDir, FontMap, DefaultFont
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend UPSD2UMGSettings with new properties and category grouping</name>
  <files>Source/PSD2UMG/Public/PSD2UMGSetting.h, Source/PSD2UMG/Private/PSD2UMGSetting.cpp</files>
  <read_first>Source/PSD2UMG/Public/PSD2UMGSetting.h, Source/PSD2UMG/Private/PSD2UMGSetting.cpp</read_first>
  <action>
Add 4 new UPROPERTY fields to UPSD2UMGSettings per D-13 and UI-SPEC category grouping:

1. `bShowPreviewDialog` — bool, default true, Category = "PSD2UMG|General", ToolTip = "Show layer preview dialog before every import. Disable for batch import workflows."
2. `bUseCommonUI` — bool, default false, Category = "PSD2UMG|CommonUI", ToolTip = "When enabled, Button_ layers generate UCommonButtonBase instead of UButton. Requires CommonUI plugin."
3. `InputActionSearchPath` — FDirectoryPath, default `/Game/Input/`, Category = "PSD2UMG|CommonUI", ToolTip = "Search path for UInputAction assets referenced in layer names (e.g. Button_Confirm[IA_Confirm])."
4. `SourceDPI` — int32, default 72, Category = "PSD2UMG|Typography", meta=(ClampMin=1), ToolTip = "Photoshop source document DPI. Used for pt to px conversion. Default 72 = 1pt equals 1px in UMG."

Re-categorize ALL existing properties with pipe-separated groups per UI-SPEC:
- bEnabled: Category = "PSD2UMG|General"
- bFlattenComplexEffects: Category = "PSD2UMG|Effects"
- MaxSmartObjectDepth: Category = "PSD2UMG|Effects"
- TextureSrcDir: Category = "PSD2UMG|Output"
- TextureAssetDir: Category = "PSD2UMG|Output"
- WidgetBlueprintAssetDir: Category = "PSD2UMG|Output"
- FontMap: Category = "PSD2UMG|Typography"
- DefaultFont: Category = "PSD2UMG|Typography"

In the constructor (PSD2UMGSetting.cpp), set `InputActionSearchPath.Path = TEXT("/Game/Input/");`.
  </action>
  <verify>
    <automated>grep -c "PSD2UMG|" Source/PSD2UMG/Public/PSD2UMGSetting.h</automated>
  </verify>
  <acceptance_criteria>
    - PSD2UMGSetting.h contains `bool bShowPreviewDialog = true`
    - PSD2UMGSetting.h contains `bool bUseCommonUI = false`
    - PSD2UMGSetting.h contains `FDirectoryPath InputActionSearchPath`
    - PSD2UMGSetting.h contains `int32 SourceDPI = 72`
    - PSD2UMGSetting.h contains `Category = "PSD2UMG|General"` on bEnabled
    - PSD2UMGSetting.h contains `Category = "PSD2UMG|Output"` on TextureAssetDir
    - PSD2UMGSetting.h contains `Category = "PSD2UMG|Typography"` on FontMap
    - PSD2UMGSetting.h contains `Category = "PSD2UMG|Effects"` on bFlattenComplexEffects
    - PSD2UMGSetting.h contains `ClampMin=1` on SourceDPI
    - PSD2UMGSetting.cpp contains `/Game/Input/`
  </acceptance_criteria>
  <done>All 12 UPROPERTY fields have pipe-separated categories, 4 new fields with correct defaults and tooltips</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 7 module dependencies to Build.cs</name>
  <files>Source/PSD2UMG/PSD2UMG.Build.cs</files>
  <read_first>Source/PSD2UMG/PSD2UMG.Build.cs</read_first>
  <action>
Add the following modules to PrivateDependencyModuleNames in Build.cs:
- "ToolMenus" — for UToolMenus Content Browser context menu extension
- "ContentBrowser" — for UContentBrowserAssetContextMenuContext
- "MovieScene" — for UMovieScene, FGuid possessable in animation creation
- "MovieSceneTracks" — for UMovieSceneFloatTrack, UMovieSceneFloatSection
- "EnhancedInput" — for UInputAction asset lookup (CUI-02)
- "CommonUI" — for UCommonButtonBase (CUI-01, guarded at runtime with FModuleManager::IsModuleLoaded)
- "InputCore" — for FKey used by CommonUI input path

Add them as a second AddRange block after the existing one, with a comment: `// Phase 7: Editor UI, preview dialog, animations, CommonUI`.
  </action>
  <verify>
    <automated>grep -c "ToolMenus\|ContentBrowser\|MovieScene\|CommonUI\|EnhancedInput" Source/PSD2UMG/PSD2UMG.Build.cs</automated>
  </verify>
  <acceptance_criteria>
    - Build.cs contains "ToolMenus"
    - Build.cs contains "ContentBrowser"
    - Build.cs contains "MovieScene"
    - Build.cs contains "MovieSceneTracks"
    - Build.cs contains "EnhancedInput"
    - Build.cs contains "CommonUI"
    - Build.cs contains "InputCore"
    - All 7 are in PrivateDependencyModuleNames (not Public)
  </acceptance_criteria>
  <done>Build.cs has all module deps needed for Phase 7 features</done>
</task>

</tasks>

<verification>
- grep for all 4 new property names in PSD2UMGSetting.h
- grep for all 7 new module names in Build.cs
- All existing properties retain their UPROPERTY macros (just re-categorized)
</verification>

<success_criteria>
- UPSD2UMGSettings has 12 total UPROPERTY fields across 5 category groups
- Build.cs compiles with all new module dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/07-editor-ui-preview-settings/07-01-SUMMARY.md`
</output>

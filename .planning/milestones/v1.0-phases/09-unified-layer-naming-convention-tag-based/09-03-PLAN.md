---
phase: 09-unified-layer-naming-convention-tag-based
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - Source/PSD2UMG/Tests/Fixtures/MultiLayer.psd
  - Source/PSD2UMG/Tests/Fixtures/Typography.psd
  - Source/PSD2UMG/Tests/Fixtures/SimpleHUD.psd
  - Source/PSD2UMG/Tests/Fixtures/ComplexMenu.psd
  - Source/PSD2UMG/Tests/Fixtures/Effects.psd
  - Source/PSD2UMG/Private/Tests/PsdParserSpec.cpp
  - Source/PSD2UMG/Private/Tests/FWidgetBlueprintGenSpec.cpp
  - Source/PSD2UMG/Private/Tests/FTextPipelineSpec.cpp
  - Source/PSD2UMG/Private/Tests/FFontResolverSpec.cpp
autonomous: false
requirements:
  - TAG-05
must_haves:
  truths:
    - "All 5 fixture PSDs use only the new @-tag grammar — no Button_, _anchor-*, [IA_X], [L,T,R,B], _9s, _variants, _show/_hide/_hover, _hovered/_pressed/_fill/_bg layer names remain"
    - "All Phase 8 specs that assert on literal layer names assert against the retagged names"
    - "Full PSD2UMG.* automation suite is green"
  artifacts:
    - path: "Source/PSD2UMG/Tests/Fixtures/*.psd"
      provides: "5 retagged fixture PSDs (binary)"
    - path: "Source/PSD2UMG/Private/Tests/PsdParserSpec.cpp"
      provides: "Updated assertions on retagged layer names"
    - path: "Source/PSD2UMG/Private/Tests/FWidgetBlueprintGenSpec.cpp"
      provides: "Updated synthetic FPsdLayer names to use tag syntax"
  key_links:
    - from: "Tests/Fixtures/*.psd"
      to: "Tests/PsdParserSpec.cpp assertions"
      via: "literal layer-name strings"
      pattern: "@button\\|@progress\\|@anchor:\\|@state:\\|@9s\\|@variants"
---

<objective>
Retag the 5 Phase 8 fixture PSDs to the new tag grammar and rewrite the spec assertions that reference legacy layer names. Bring the full PSD2UMG automation suite back to green after the Plan 02 refactor.

Purpose: Closes the loop on Plan 02's expected red specs. Validates that the parser + refactored mappers produce correct widget trees end-to-end against real PSD binaries. Ships D-24 (retagged fixtures).

Output: 5 modified PSD binaries (hand-authored in Photoshop), 4 modified spec files, full automation suite green.

**Fixture inventory confirmed 2026-04-14** (via `ls Source/PSD2UMG/Tests/Fixtures/`):
- `ComplexMenu.psd` ✓
- `Effects.psd` ✓
- `MultiLayer.psd` ✓
- `SimpleHUD.psd` ✓
- `Typography.psd` ✓

All 5 fixtures from Phase 8 exist on disk; no additions or removals. The `files_modified` list is authoritative.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-unified-layer-naming-convention-tag-based/09-CONTEXT.md
@.planning/phases/09-unified-layer-naming-convention-tag-based/09-RESEARCH.md
@.planning/phases/09-unified-layer-naming-convention-tag-based/09-02-SUMMARY.md
@Docs/LayerTagGrammar.md
@Source/PSD2UMG/Private/Tests/PsdParserSpec.cpp
@Source/PSD2UMG/Private/Tests/FWidgetBlueprintGenSpec.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft per-PSD retag checklist</name>
  <files>
    .planning/phases/09-unified-layer-naming-convention-tag-based/09-03-RETAG-CHECKLIST.md (NEW, planning artifact — safe to create under .planning/)
  </files>
  <action>
    Implements D-24 preparation. Enumerates exactly which layers in each fixture PSD need renaming.

    1. For each of the 5 fixture PSDs (MultiLayer, Typography, SimpleHUD, ComplexMenu, Effects), read the current layer-name assertions from the spec files and the 09-RESEARCH.md §Migration Scope table.

    2. Produce a markdown checklist with one section per PSD, each row showing `old layer name` → `new tag-form name`. Example rows:
       - `Button_Start` → `Start @button`
       - `Background_fill` → `Background @state:fill` (if it's a progress-bar child) OR `Background @image` (if standalone)
       - `Menu_variants` → `Menu @variants`
       - `Icon_anchor-tl` → `Icon @anchor:tl`
       - `Panel_9s[16,16,16,16]` → `Panel @9s:16,16,16,16`
       - `Progress_Health` → `Health @progress`
       - `Button_Confirm[IA_Confirm]` → `Confirm @button @ia:IA_Confirm`
       - `FadeIn_show` → `FadeIn @anim:show`

    3. Include a "General lint" section per D-22/D-23 referencing Docs/LayerTagGrammar.md: no trailing `_` suffixes; no `[...]` brackets in names; only one type tag per layer.

    4. Also list the expected generated widget FName after retag (this is what the specs will assert against) — per identity strategy decided in Plan 02 Task 1.

    Commit: `docs(09-03): fixture PSD retag checklist`
  </action>
  <verify><automated>ls .planning/phases/09-unified-layer-naming-convention-tag-based/09-03-RETAG-CHECKLIST.md</automated></verify>
  <done>Checklist file exists listing every old→new layer name per PSD; widget FName expectations documented.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Retag the 5 fixture PSDs in Photoshop</name>
  <what-built>Retag checklist from Task 1 is ready. The 5 binary PSDs at Source/PSD2UMG/Tests/Fixtures/ still contain old-syntax layer names.</what-built>
  <how-to-verify>
    This is genuinely manual — PhotoshopAPI in this project is read-only; there is no CLI/API path to rename PSD layers programmatically. (Deferred per CONTEXT.md — adding write support is explicitly out of scope.)

    For each of: MultiLayer.psd, Typography.psd, SimpleHUD.psd, ComplexMenu.psd, Effects.psd:
    1. Open in Photoshop.
    2. Work through the row-by-row rename list in 09-03-RETAG-CHECKLIST.md.
    3. Save the PSD (overwrite).
    4. Git diff will show binary changes only — that's expected.

    Sanity check per PSD after save:
    - Open Unreal editor, drag the PSD in, open preview dialog. All layer names in the tree view should now contain `@`-tokens. No `Button_`, `_9s`, `[IA_X]` etc. visible anywhere.
  </how-to-verify>
  <resume-signal>Type "approved" after all 5 PSDs are retagged and saved, OR describe which PSDs are blocked and why.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite spec assertions to match retagged fixtures + update synthetic layer names</name>
  <files>
    Source/PSD2UMG/Private/Tests/PsdParserSpec.cpp,
    Source/PSD2UMG/Private/Tests/FWidgetBlueprintGenSpec.cpp,
    Source/PSD2UMG/Private/Tests/FTextPipelineSpec.cpp,
    Source/PSD2UMG/Private/Tests/FFontResolverSpec.cpp
  </files>
  <behavior>
    Every assertion that references legacy-named layers (`Button_Start`, `Progress_Health`, `Menu_variants`, `Background_fill`, `Icon_anchor-tl`, `Panel_9s`, etc.) is updated to either:
    - (for fixture-driven tests) assert against the new expected widget FName per the identity strategy (option-a: clean name like `Start`; option-b: original tagged name like `Start @button` as identity-key, but widget display name still `Start`).
    - (for synthetic-layer tests) rewrite `.Name = TEXT("Button_Start")` to `.Name = TEXT("Start @button")` so the parser populates ParsedTags.Type=Button (via the MakeTaggedTestLayer helper added in Plan 02 Task 5).

    Full PSD2UMG.* spec suite green after this task.
  </behavior>
  <action>
    Implements TAG-05 + resolves red-test inventory from 09-02-SUMMARY.

    1. Open each spec file. Grep for legacy patterns: `Button_`, `Progress_`, `HBox_`, `VBox_`, `Overlay_`, `ScrollBox_`, `Slider_`, `CheckBox_`, `Input_`, `List_`, `Tile_`, `Switcher_`, `_anchor-`, `_stretch-`, `_9s`, `_9slice`, `_variants`, `_hovered`, `_pressed`, `_fill`, `_bg`, `_show`, `_hide`, `_hover`, `[IA_`, `[` followed by digit-comma-digit (9-slice margins).

    2. For each hit, apply the corresponding replacement from 09-03-RETAG-CHECKLIST.md. Example rewrites (approximate — exact lines per 09-RESEARCH.md):
       - PsdParserSpec.cpp line ~360-373: assertion on `Progress_Health` → assertion on `Health` (or tagged form per identity strategy); asserted layer name in parsed doc becomes `Health @progress`.
       - FWidgetBlueprintGenSpec.cpp line ~380-386 (`Button_`): synthetic `.Name = TEXT("Button_Start")` → `.Name = TEXT("Start @button")`.
       - FWidgetBlueprintGenSpec.cpp line ~401-407 (`Progress_Health`): `.Name = TEXT("Health @progress")`.
       - FWidgetBlueprintGenSpec.cpp line ~422-428 (`HBox_Row`): `.Name = TEXT("Row @hbox")`.
       - FWidgetBlueprintGenSpec.cpp line ~443-449 (`Menu_variants`): `.Name = TEXT("Menu @variants")`.
       - FWidgetBlueprintGenSpec.cpp line ~470-476 (`Background_fill`): depends on context — if it's a progress child, `.Name = TEXT("Background @state:fill")`; if it's a standalone image, `.Name = TEXT("Background @image")`. Research indicates it's a progress fill; use `@state:fill`.
       - FWidgetBlueprintGenSpec.cpp line ~498-504 (`Icon_anchor-tl`): `.Name = TEXT("Icon @anchor:tl")` with Type=Image or Group matching original.
       - FWidgetBlueprintGenSpec.cpp line ~642-648 (`Panel_9s`): `.Name = TEXT("Panel @9s")` (shorthand — uses 16px default) or `.Name = TEXT("Panel @9s:16,16,16,16")` if the test asserts specific margins.

    3. For FTextPipelineSpec.cpp and FFontResolverSpec.cpp: verify via grep whether they touch layer-name dispatch. Per research, likely minor/none. If any legacy references exist, apply same rewrite.

    4. Run full automation suite. Fix any remaining assertion mismatches (expected: tests that were green before Plan 02 refactor and then red after Plan 02 should be green again).

    Commit pattern: `test(09-03): retag spec assertions for {SpecFileName}`
  </action>
  <verify>
    <automated>"$UE_ROOT/Engine/Binaries/Win64/UnrealEditor-Cmd.exe" "C:/Dev/psd-to-umg/PSD2UMG.uproject" -ExecCmds="Automation RunTests PSD2UMG.; quit" -unattended -nopause -log 2>&amp;1 | tail -60</automated>
  </verify>
  <done>All PSD2UMG.* automation tests green. Zero legacy prefix/suffix/bracket strings in Tests/*.cpp.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual smoke-import each retagged fixture</name>
  <what-built>5 retagged PSDs + all specs green against them.</what-built>
  <how-to-verify>
    Per 09-VALIDATION.md Manual-Only Verifications:
    1. In UE editor, drag each of the 5 fixture PSDs into the Content Browser.
    2. Confirm import via the preview dialog.
    3. Open the generated WBP in UMG Designer.
    4. Compare against the Phase 8 baseline (layer tree structure + widget types + approximate layout) — the tag refactor should not have changed the resulting widget tree, only the layer-name syntax that produces it.
    5. Confirm no errors in Output Log; warnings are acceptable if they come from deliberately-unknown tags.
  </how-to-verify>
  <resume-signal>Type "approved" or describe per-PSD discrepancies (expected vs. actual widget tree).</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn "Button_\\|Progress_\\|HBox_\\|VBox_\\|Overlay_\\|ScrollBox_\\|Slider_\\|CheckBox_\\|Input_\\|List_\\|Tile_\\|Switcher_\\|_anchor-\\|_stretch-\\|_9s\\|_9slice\\|_variants\\|_hovered\\|_pressed\\|_hover\\|_show\\|_hide\\|_fill\\|_bg\\|\\[IA_" Source/PSD2UMG/` returns zero hits (in source AND tests — fixtures are binary so grep-exempt).
- Full PSD2UMG.* automation suite green.
- Human smoke test passes for all 5 fixtures.
</verification>

<success_criteria>
- 5 fixture PSDs retagged, committed as binary blobs.
- 4 spec files updated; all assertions use tag syntax.
- Full automation suite green.
- Human smoke test approved.
</success_criteria>

<risks>
- **R-07 (Research Risk #1):** Manual Photoshop work is error-prone. The checklist in Task 1 is the mitigation — reviewer cross-checks every row against the diff before approving Task 2.
- **R-05 carry-over (Anchor heuristic + explicit stretch):** If Plan 02 Task 6 smoke test surfaced this issue, Task 3 must also add a spec case covering a stretch-h child inside an auto-detected row. Flag in SUMMARY.
- **Identity-key strategy delta:** If Plan 02 Task 1 chose option-b (store original Layer.Name as metadata), Task 3 spec assertions may need to check the metadata rather than the widget FName. Align with the SUMMARY from Plan 02 before writing assertions.
</risks>

<output>
Create `.planning/phases/09-unified-layer-naming-convention-tag-based/09-03-SUMMARY.md`: which PSDs were retagged, spec-rewrite scope, final test counts (green/red/skipped), smoke-test outcome per PSD.
</output>

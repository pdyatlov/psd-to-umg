---
phase: 09-unified-layer-naming-convention-tag-based
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - README.md
  - Docs/Migration-PrefixToTag.md
autonomous: true
requirements:
  - TAG-04
  - TAG-06
must_haves:
  truths:
    - "README.md naming cheat sheet shows ONLY the new @-tag grammar — no legacy prefix/suffix/bracket examples remain"
    - "Docs/Migration-PrefixToTag.md enumerates every removed legacy pattern with its @-tag replacement and before/after PSD layer-name examples"
  artifacts:
    - path: "Docs/Migration-PrefixToTag.md"
      provides: "Designer-facing migration guide (D-23)"
    - path: "README.md"
      provides: "Updated naming cheat sheet (D-25)"
  key_links:
    - from: "README.md naming section"
      to: "Docs/LayerTagGrammar.md"
      via: "cross-reference link for the formal grammar"
      pattern: "LayerTagGrammar.md"
    - from: "Docs/Migration-PrefixToTag.md"
      to: "Docs/LayerTagGrammar.md"
      via: "link for forward reference to the new syntax spec"
      pattern: "LayerTagGrammar.md"
---

<objective>
Ship the two designer-facing documentation deliverables: the migration guide (D-23) mapping every removed legacy pattern to its new tag equivalent, and the README rewrite (D-25) showing only the new grammar. This closes the user-facing side of the hard cutover.

Purpose: Without these docs, designers cannot retag their own PSDs and existing users of the plugin have no migration path. This is the final phase artifact.

Output: README.md updated; Docs/Migration-PrefixToTag.md created; cross-references to Docs/LayerTagGrammar.md (from Plan 01) wired in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-unified-layer-naming-convention-tag-based/09-CONTEXT.md
@.planning/phases/09-unified-layer-naming-convention-tag-based/09-RESEARCH.md
@Docs/LayerTagGrammar.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Docs/Migration-PrefixToTag.md (D-23)</name>
  <files>Docs/Migration-PrefixToTag.md (NEW)</files>
  <action>
    Implements D-23 + TAG-04.

    Structure:

    1. **Header** — "Migration Guide: Prefix Syntax → @-Tag Grammar", with intro paragraph: hard cutover in Phase 9; no backwards compatibility; existing PSDs must be retagged by hand; this guide is exhaustive.

    2. **Old → New Mapping Table** (one row per removed pattern — data from 09-RESEARCH.md §State of the Art "Deprecated/removed" list + §Migration Scope):

       | Old syntax | New syntax | Example old | Example new |
       |------------|------------|-------------|-------------|
       | `Button_` prefix | `@button` | `Button_Start` | `Start @button` |
       | `Progress_` prefix | `@progress` | `Progress_Health` | `Health @progress` |
       | `HBox_` prefix | `@hbox` | `HBox_Row` | `Row @hbox` |
       | `VBox_` prefix | `@vbox` | `VBox_Column` | `Column @vbox` |
       | `Overlay_` prefix | `@overlay` | `Overlay_Popup` | `Popup @overlay` |
       | `ScrollBox_` prefix | `@scrollbox` | `ScrollBox_List` | `List @scrollbox` |
       | `Slider_` prefix | `@slider` | `Slider_Volume` | `Volume @slider` |
       | `CheckBox_` prefix | `@checkbox` | `CheckBox_Mute` | `Mute @checkbox` |
       | `Input_` prefix | `@input` | `Input_Name` | `Name @input` |
       | `List_` prefix | `@list` | `List_Items` | `Items @list` |
       | `Tile_` prefix | `@tile` | `Tile_Grid` | `Grid @tile` |
       | `Switcher_` prefix | `@variants` (merged with `_variants`) | `Switcher_Pages` | `Pages @variants` |
       | `_variants` suffix | `@variants` | `Menu_variants` | `Menu @variants` |
       | `_9s` / `_9slice` suffix | `@9s` (shorthand, 16px default) | `Panel_9s` | `Panel @9s` |
       | `_9s[L,T,R,B]` bracket | `@9s:L,T,R,B` | `Panel_9s[8,16,8,16]` | `Panel @9s:8,16,8,16` |
       | `_anchor-tl` etc. | `@anchor:tl` etc. | `Icon_anchor-tl` | `Icon @anchor:tl` |
       | `_stretch-h` / `_stretch-v` | `@anchor:stretch-h` / `@anchor:stretch-v` | `Bar_stretch-h` | `Bar @anchor:stretch-h` |
       | `_fill` (anchor) | `@anchor:fill` | `Bg_fill` | `Bg @anchor:fill` |
       | `_hovered` child suffix | `@state:hover` | `Start_hovered` | `Hover @state:hover` |
       | `_pressed` child suffix | `@state:pressed` | `Start_pressed` | `Pressed @state:pressed` |
       | `_disabled` child suffix | `@state:disabled` | `Start_disabled` | `Disabled @state:disabled` |
       | `_normal` child suffix | `@state:normal` | `Start_normal` | `Normal @state:normal` |
       | `_fill` (progress child) | `@state:fill` | `Health_fill` | `Fill @state:fill` |
       | `_bg` / `_background` child | `@state:bg` | `Health_bg` | `Bg @state:bg` |
       | `[IA_X]` bracket | `@ia:IA_X` | `Button_Confirm[IA_Confirm]` | `Confirm @button @ia:IA_Confirm` |
       | `_show` anim child | `@anim:show` | `Panel_show` | `ShowAnim @anim:show` |
       | `_hide` anim child | `@anim:hide` | `Panel_hide` | `HideAnim @anim:hide` |
       | `_hover` anim child | `@anim:hover` | `Panel_hover` | `HoverAnim @anim:hover` |
       | `Btn_` prefix (preview-only) | `@button` | `Btn_OK` | `OK @button` |

    3. **Before / After PSD Walkthrough** — a ~15-layer example tree (similar to SimpleHUD) shown in full before-and-after, so designers can copy-paste the rewrite pattern.

    4. **"Lint your PSD" Checklist** (per 09-RESEARCH.md §Common Pitfall 1):
       - Search all layer names for `_` suffixes that match any row in the table above.
       - Confirm every widget-producing layer has exactly one type tag.
       - Confirm state-child layers carry `@state:*`.
       - Confirm no `[...]` brackets remain — all bracket params are now tag values.
       - Confirm widget-display names (text before first `@`) are FName-valid (no `/`, `.`, `\`, `:`).

    5. **Reimport Identity Warning Box** — per Plan 02 Task 1 decision (option-a or option-b). State the consequence: existing pre-Phase-9 WBPs may orphan on reimport; recommend delete + full reimport from retagged PSD.

    6. **Cross-reference** link to Docs/LayerTagGrammar.md for formal spec.

    Commit: `docs(09-04): add migration guide Prefix→Tag`
  </action>
  <verify><automated>ls Docs/Migration-PrefixToTag.md &amp;&amp; grep -q "@button" Docs/Migration-PrefixToTag.md &amp;&amp; grep -q "@state:" Docs/Migration-PrefixToTag.md &amp;&amp; grep -q "LayerTagGrammar" Docs/Migration-PrefixToTag.md &amp;&amp; echo OK</automated></verify>
  <done>Migration guide exists, covers every removed pattern, includes walkthrough + lint checklist + identity warning + cross-reference.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md naming section (D-25)</name>
  <files>README.md</files>
  <action>
    Implements D-25 + TAG-06.

    1. Read current README.md and locate the layer-naming cheat sheet (research notes ~lines 39-96; verify exact range by reading the file).

    2. Replace that section with a terse tag-based cheat sheet. Keep it short — the formal spec lives at Docs/LayerTagGrammar.md. Recommended content:

       ```markdown
       ## Layer Naming

       PSD layer names drive widget generation via @-tags. Format:

           WidgetName @tag @tag:value @tag:v1,v2,...

       The widget name is whatever text appears before the first `@`. Everything after is parsed as tags.

       ### Type tags (one per layer)
       | Tag | Widget |
       |-----|--------|
       | @button | UButton (or UCommonButtonBase in CommonUI mode) |
       | @image | UImage |
       | @text | UTextBlock |
       | @progress | UProgressBar |
       | @hbox / @vbox | UHorizontalBox / UVerticalBox |
       | @overlay | UOverlay |
       | @scrollbox | UScrollBox |
       | @slider | USlider |
       | @checkbox | UCheckBox |
       | @input | UEditableTextBox |
       | @list / @tile | UListView / UTileView |
       | @variants | UWidgetSwitcher |
       | @smartobject[:TypeName] | child UUserWidget |
       | @canvas | UCanvasPanel (default for groups) |

       Default types: group layers → `@canvas`, pixel layers → `@image`, text layers → `@text`.

       ### Modifier tags
       | Tag | Effect |
       |-----|--------|
       | @anchor:tl / tc / tr / cl / c / cr / bl / bc / br | Explicit anchor corner/center |
       | @anchor:stretch-h / stretch-v / fill | Stretch modes |
       | @9s / @9s:L,T,R,B | 9-slice (default 16px on all sides if no margins) |
       | @ia:IA_ActionName | Bind CommonUI input action |
       | @state:hover / pressed / disabled / normal / fill / bg | State-brush child |
       | @anim:show / hide / hover | Animation variant (inside CommonUI button) |

       ### Rules
       - Tags are case-insensitive. Values that reference asset names (`@ia:IA_Confirm`) preserve case.
       - Tags are order-free. Conflicting tags: last wins + warning logged.
       - Unknown tags: warning + ignored.
       - Empty widget name (`@button` alone): auto-named `Button_NN`.

       ### Examples
       - `PlayButton @button @anchor:tl`
       - `Health @progress`  (with children `Fill @state:fill` and `Bg @state:bg`)
       - `Confirm @button @ia:IA_Confirm`
       - `Panel @9s:16,16,16,16`
       - `Menu @variants`  (children are variant pages)

       Formal grammar: [Docs/LayerTagGrammar.md](Docs/LayerTagGrammar.md).
       Migration from pre-Phase-9 syntax: [Docs/Migration-PrefixToTag.md](Docs/Migration-PrefixToTag.md).
       ```

    3. Verify no legacy examples remain anywhere else in README (search for `Button_`, `_anchor-`, `[IA_`, `_9s`, `_hovered`, `_variants`, `_show`, `_hide` etc. elsewhere in the file and rewrite or delete).

    Commit: `docs(09-04): rewrite README layer-naming section for tag grammar`
  </action>
  <verify><automated>grep -qE "Button_|_anchor-|\\[IA_|_9s|_hovered|_variants[^d]" README.md &amp;&amp; echo LEGACY_FOUND_FAIL || echo OK</automated></verify>
  <done>README naming section shows only tag grammar; zero legacy prefix/suffix/bracket examples remain in README.md; cross-references to Docs/LayerTagGrammar.md and Docs/Migration-PrefixToTag.md present.</done>
</task>

</tasks>

<verification>
- `grep -rnE "Button_|Progress_|HBox_|VBox_|Overlay_|ScrollBox_|Slider_|CheckBox_|Input_|List_|Tile_|Switcher_|_anchor-|_stretch-|_9s|_9slice|_variants|_hovered|_pressed|_hover|_show|_hide|\\[IA_" README.md Docs/ 2>/dev/null | grep -v "Migration-PrefixToTag.md"` returns zero hits (the migration guide is the only legitimate place these strings survive, as the "old syntax" column).
- Full PSD2UMG.* automation suite still green (no code touched, sanity check).
</verification>

<success_criteria>
- Docs/Migration-PrefixToTag.md shipped with full old→new table, walkthrough, lint checklist, identity-key warning, cross-reference.
- README.md naming section replaced; only tag grammar shown; cross-references wired.
- All Phase 9 deliverables (D-01..D-25) now fulfilled across plans 01-04.
</success_criteria>

<output>
Create `.planning/phases/09-unified-layer-naming-convention-tag-based/09-04-SUMMARY.md`: docs shipped, cross-reference map, final Phase 9 acceptance checklist (every D-XX in CONTEXT.md marked complete with plan+task citation).
</output>

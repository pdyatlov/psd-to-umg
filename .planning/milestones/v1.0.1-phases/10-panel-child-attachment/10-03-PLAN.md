---
phase: 10-panel-child-attachment
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - Source/PSD2UMG/Tests/Fixtures/Panels.psd
  - Source/PSD2UMG/Tests/FPanelAttachmentSpec.cpp
  - Source/PSD2UMG/PSD2UMG.Build.cs
autonomous: false
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04, PANEL-05, PANEL-06, PANEL-07]
must_haves:
  truths:
    - "Panels.psd fixture exists under Source/PSD2UMG/Tests/Fixtures/ with VBoxGroup, HBoxGroup, ScrollBoxGroup, OverlayGroup, CanvasGroup, and a nested OuterVBox > InnerCanvas > NestedItem group structure"
    - "FPanelAttachmentSpec compiles and runs under WITH_DEV_AUTOMATION_TESTS"
    - "When Panels.psd is absent, every spec case passes silently (fixture-gated via FPaths::FileExists) — matches FTextEffectsSpec pattern"
    - "When Panels.psd is present, the spec asserts widget class per group, GetChildrenCount() per group, child names + widget classes, canvas anchor/offset preservation for CanvasGroup, and nested-case dispatch correctness"
    - "Human has authored and committed Panels.psd binary (checkpoint)"
  artifacts:
    - path: "Source/PSD2UMG/Tests/Fixtures/Panels.psd"
      provides: "Binary fixture exercising @vbox, @hbox, @scrollbox, @overlay, @canvas, and nested mixed group"
    - path: "Source/PSD2UMG/Tests/FPanelAttachmentSpec.cpp"
      provides: "Automation spec for PANEL-01..PANEL-07"
      contains: "FPanelAttachmentSpec"
      min_lines: 150
  key_links:
    - from: "FPanelAttachmentSpec"
      to: "Panels.psd fixture"
      via: "FPaths::FileExists gate + FPsdParser parse + FWidgetBlueprintGenerator::Generate"
      pattern: "FPaths::FileExists"
    - from: "FPanelAttachmentSpec"
      to: "PopulateChildren dispatch logic (Plan 10-01)"
      via: "Generate produces WBP; spec Cast<UVerticalBox/UHorizontalBox/UScrollBox/UOverlay> on widget tree"
      pattern: "Cast<UVerticalBox>|Cast<UHorizontalBox>|Cast<UScrollBox>|Cast<UOverlay>"
---

<objective>
Author the `Panels.psd` fixture (human-in-the-loop Photoshop step) and `FPanelAttachmentSpec` automation spec that together cover PANEL-01..PANEL-07. The spec asserts child counts and widget class per nested group type, uses the `FPaths::FileExists` fixture-gate pattern from `FTextEffectsSpec`, and serves as the regression harness for the 10-01 Generate-path refactor.

Purpose: PANEL-07 explicitly requires the fixture + spec. Without it, the other requirements have no automated proof and future refactors could silently re-break non-canvas attachment.

Output:
- `Source/PSD2UMG/Tests/Fixtures/Panels.psd` committed as binary.
- `Source/PSD2UMG/Tests/FPanelAttachmentSpec.cpp` compiled under `WITH_DEV_AUTOMATION_TESTS`.
- Build.cs updated if the Tests directory needs a new include or UMG dependency (likely already present — verify).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-panel-child-attachment/10-CONTEXT.md
@.planning/phases/10-panel-child-attachment/10-01-PLAN.md
@.planning/phases/10-panel-child-attachment/10-01-SUMMARY.md

@Source/PSD2UMG/Tests/FTextEffectsSpec.cpp
@Source/PSD2UMG/PSD2UMG.Build.cs

<interfaces>
Fixture-gate pattern (from FTextEffectsSpec):

```cpp
FixturePath = FPaths::Combine(
    IPluginManager::Get().FindPlugin(TEXT("PSD2UMG"))->GetBaseDir(),
    TEXT("Source/PSD2UMG/Tests/Fixtures/Panels.psd"));
bFixtureExists = FPaths::FileExists(FixturePath);

It("VBoxGroup attaches 3 children", [this]()
{
    if (!bFixtureExists) { return; } // silent skip — fixture not authored yet
    // ...assertions...
});
```

Widget types (all in `Components/`):
- UVerticalBox, UHorizontalBox, UScrollBox, UOverlay (all UPanelWidget subclasses)
- UCanvasPanel (also UPanelWidget subclass)

Key WBP traversal API:
```cpp
UWidgetTree* WBP->WidgetTree;
Tree->ForEachWidget([](UWidget* W){ ... });
Tree->FindWidget(FName);
UPanelWidget::GetChildrenCount(), GetChildAt(int32);
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Author Panels.psd fixture in Photoshop (human checkpoint)</name>
  <what-to-build>
    Create `Source/PSD2UMG/Tests/Fixtures/Panels.psd` with the following structure per D-12:

    Root (canvas 1024x1024, RGB 8-bit):
    - Group "VBoxGroup @vbox"
        - Text layer "ItemA @text" (any text, any position)
        - Image/pixel layer "ItemB @image" (any small rect)
        - Group "ItemC @button" containing one image layer named "ItemC_Bg @image"
    - Group "HBoxGroup @hbox"
        - Text layer "H1 @text"
        - Text layer "H2 @text"
    - Group "ScrollBoxGroup @scrollbox"
        - Text layer "S1 @text"
        - Text layer "S2 @text"
        - Text layer "S3 @text"
        - Text layer "S4 @text"
    - Group "OverlayGroup @overlay"
        - Image layer "O_Back @image"
        - Text layer "O_Front @text"
    - Group "CanvasGroup @canvas"
        - Text layer "C1 @text" at a recognisable position (e.g. x=50, y=50, w=100, h=40) — regression anchor
        - Image layer "C2 @image" at (200,200,300,250)
    - Group "OuterVBox @vbox"
        - Group "InnerCanvas @canvas"
            - Text layer "NestedItem @text" at (10,10,100,40)

    Save as PSD (Photoshop format, maximize compatibility on).
  </what-to-build>
  <how-to-verify>
    1. Open the authored PSD and confirm each group name matches exactly (including the tag and space).
    2. Confirm `Source/PSD2UMG/Tests/Fixtures/Panels.psd` exists on disk and is committed.
    3. Optional smoke: run the existing PSD parser against it and eyeball logs for 6 top-level groups and the nested OuterVBox > InnerCanvas > NestedItem path.
  </how-to-verify>
  <resume-signal>Type "fixture committed" or describe what's missing.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement FPanelAttachmentSpec.cpp</name>
  <files>
    Source/PSD2UMG/Tests/FPanelAttachmentSpec.cpp
    Source/PSD2UMG/PSD2UMG.Build.cs
  </files>
  <action>
    1. Check `PSD2UMG.Build.cs` includes `"UMG"` in its dependency list (should already be present from Phase 3). Add if missing. No other build changes expected.

    2. Create `Source/PSD2UMG/Tests/FPanelAttachmentSpec.cpp` modeled on `FTextEffectsSpec.cpp`:

       ```cpp
       #if WITH_DEV_AUTOMATION_TESTS
       #include "Misc/AutomationTest.h"
       #include "Misc/Paths.h"
       #include "Interfaces/IPluginManager.h"
       #include "Parser/PsdParser.h"
       #include "Parser/PsdTypes.h"
       #include "Parser/PsdDiagnostics.h"
       #include "Generator/FWidgetBlueprintGenerator.h"
       #include "WidgetBlueprint.h"
       #include "Blueprint/WidgetTree.h"
       #include "Components/CanvasPanel.h"
       #include "Components/CanvasPanelSlot.h"
       #include "Components/VerticalBox.h"
       #include "Components/HorizontalBox.h"
       #include "Components/ScrollBox.h"
       #include "Components/Overlay.h"
       #include "Components/TextBlock.h"
       #include "Components/Image.h"
       ```

    3. Spec skeleton with BEGIN_DEFINE_SPEC / END_DEFINE_SPEC and the following members:
       ```cpp
       FString FixturePath;
       bool bFixtureExists = false;
       bool bGenerated = false;
       UWidgetBlueprint* WBP = nullptr;
       ```

    4. In `Define()`:
       - BeforeEach: compute FixturePath via IPluginManager (same as FTextEffectsSpec), set bFixtureExists. If exists, parse via `FPsdParser` and call `FWidgetBlueprintGenerator::Generate(Doc, TEXT("/Engine/Transient"), TEXT("WBP_PanelsSpec"))`. Store WBP pointer. (If generate throws or returns null, AddError.)
       - AfterEach: cleanup — detach WBP, mark for GC (pattern from existing specs).
       - Define test cases (each gated on `if (!bFixtureExists) return;`):

    5. Test cases (all fixture-gated, per D-13 / D-14):

       **VBoxGroup_IsVerticalBoxWith3Children** (PANEL-01):
       - `FindWidget("VBoxGroup")` returns non-null.
       - `Cast<UVerticalBox>(w)` succeeds.
       - `GetChildrenCount() == 3`.
       - Children include `ItemA` (UTextBlock), `ItemB` (UImage), `ItemC` (UPanelWidget/button container — class depends on mapper; assert non-null and not-UCanvasPanelSlot on its slot).

       **HBoxGroup_IsHorizontalBoxWith2Children** (PANEL-02):
       - Cast to UHorizontalBox, count == 2.

       **ScrollBoxGroup_IsScrollBoxWith4Children** (PANEL-03):
       - Cast to UScrollBox, count == 4.

       **OverlayGroup_IsOverlayWith2Children** (PANEL-04):
       - Cast to UOverlay, count == 2.

       **CanvasGroup_PreservesAnchorsAndOffsets** (PANEL-05 regression):
       - Cast to UCanvasPanel, count == 2.
       - Spot-check one child: `C1`'s Slot is `UCanvasPanelSlot` with `Offsets.Left` within tolerance of PSD x.

       **NestedMixedGroups_DispatchCorrectly** (PANEL-06 + recursion correctness):
       - `OuterVBox` → UVerticalBox, count == 1.
       - `InnerCanvas` → UCanvasPanel, count == 1.
       - `NestedItem` → UTextBlock; its Slot is a UCanvasPanelSlot with non-zero offsets (proves canvas branch still applies positional data inside a canvas parent that is itself inside a vbox).

       **NonCanvasChildSlotsHaveNoPositionalData** (PANEL-01..04 negative):
       - For `ItemA` inside VBoxGroup, its Slot must NOT be a UCanvasPanelSlot (it should be UVerticalBoxSlot).

       **ReimportPreservesNonCanvasChildren** (PANEL-01..04 reimport regression sentinel; covers 10-02 UpdateCanvas non-canvas branch):
       - In BeforeEach, after the initial `Generate()` captures `WBP_PanelsSpec`, immediately call `FWidgetBlueprintGenerator::Update(WBP, Doc, SkippedLayerNames)` on the same WBP (no PSD change — trivial no-op reimport).
       - Re-fetch `VBoxGroup` and `ScrollBoxGroup` from the widget tree.
       - Assert `GetChildrenCount()` is unchanged (3 for VBox, 4 for ScrollBox).
       - Assert the first child (`ItemA`) is still a UTextBlock and its slot is still a `UVerticalBoxSlot` — confirms clear-and-rebuild preserved class identity.
       - This is the only reimport-path regression sentinel; no assertion on object identity pointers (clear-and-rebuild replaces instances by design).

    6. Use the same small helpers as FTextEffectsSpec: `FindWidgetByName(Tree, FName)` via `Tree->FindWidget`, local `FindLayerByName` for diagnostics. Tolerances: 1.0f on positional floats.

    7. Compile clean under the existing test runner. No new modules or build flags.
  </action>
  <verify>
    <automated>cd C:/Dev/psd-to-umg &amp;&amp; "C:/Program Files/Epic Games/UE_5.7/Engine/Build/BatchFiles/Build.bat" PSD2UMGEditor Win64 Development -Project="C:/Path/To/HostProject.uproject" -WaitMutex -FromMsBuild</automated>
    Runtime automation: `UnrealEditor-Cmd.exe HostProject.uproject -ExecCmds="Automation RunTests PSD2UMG.PanelAttachment;Quit" -unattended -nopause -testexit="Automation Test Queue Empty"` — expect all 7 spec cases to PASS (with fixture present) or SKIP-silently (if fixture missing, per gate).
  </verify>
  <done>
    - FPanelAttachmentSpec.cpp compiles.
    - All 7 test cases present and guarded by bFixtureExists.
    - When fixture present, every case passes against the 10-01 refactored Generate path.
    - No changes outside files_modified.
    - PANEL-07 artifact exists.
  </done>
</task>

</tasks>

<verification>
### Dimension 8 — Nyquist Requirement/Test Mapping

| Requirement | Fixture layer / group | Spec case |
|---|---|---|
| PANEL-01 (@vbox → UVerticalBox, children in z-order) | VBoxGroup @vbox | VBoxGroup_IsVerticalBoxWith3Children |
| PANEL-02 (@hbox → UHorizontalBox) | HBoxGroup @hbox | HBoxGroup_IsHorizontalBoxWith2Children |
| PANEL-03 (@scrollbox → UScrollBox) | ScrollBoxGroup @scrollbox | ScrollBoxGroup_IsScrollBoxWith4Children |
| PANEL-04 (@overlay → UOverlay) | OverlayGroup @overlay | OverlayGroup_IsOverlayWith2Children |
| PANEL-05 (Canvas byte-identical) | CanvasGroup @canvas | CanvasGroup_PreservesAnchorsAndOffsets |
| PANEL-06 (no silent drops) | All — spec counts match PSD structure; manual log-grep confirms no Error emissions in the happy path and Error emits on intentional bad fixture (optional stretch) | All cases + NestedMixedGroups_DispatchCorrectly |
| PANEL-01..04 reimport (10-02 sentinel) | VBoxGroup + ScrollBoxGroup re-examined after Update() | ReimportPreservesNonCanvasChildren |
| PANEL-07 (fixture + spec exist) | Panels.psd + FPanelAttachmentSpec.cpp | File presence |

### Overall checks
- Panels.psd committed (binary in Fixtures dir, tracked by git like TextEffects.psd, Layers.psd).
- FPanelAttachmentSpec.cpp compiles in editor Development build.
- Automation test discovery finds `PSD2UMG.PanelAttachment` namespace.
- With fixture present, all cases pass against PR from 10-01 (and 10-02 for any reimport assertions if added as stretch).
- Without fixture, spec silent-passes (matches FTextEffectsSpec convention).
</verification>

<success_criteria>
- Panels.psd human-authored with the 6 top-level groups + nested case from D-12.
- FPanelAttachmentSpec covers all 7 requirements (PANEL-01..PANEL-07) with fixture-gated cases.
- Spec exercises the full pipeline: parse → generate → widget-tree traversal → type + count + slot assertions.
- Compiles clean under WITH_DEV_AUTOMATION_TESTS.
</success_criteria>

<output>
After completion, create `.planning/phases/10-panel-child-attachment/10-03-SUMMARY.md` documenting:
- Fixture authoring outcome (human step) and size/layer count observed.
- Spec file line count and test case list.
- Any surprises (e.g., button mapper returning unexpected class — adjust assertions and document).
- Link to the automation test output if captured.
</output>

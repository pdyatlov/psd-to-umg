---
phase: 22-stroke-rendering
plan: 02
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp
  - Source/PSD2UMG/Private/Mapper/FShapeLayerMapper.cpp
  - Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp
autonomous: true
requirements:
  - STROKE-01
  - STROKE-03
must_haves:
  truths:
    - "An Image layer with Effects.bHasStroke=true on a canvas parent produces an additional UImage sibling whose name contains '_Stroke', whose ZOrder is exactly main ZOrder - 1, and whose canvas-slot offsets are shifted (-StrokePx, -StrokePx) and expanded (Offsets.Right += 2*StrokePx, Offsets.Bottom += 2*StrokePx) from the main widget's slot (STROKE-01)"
    - "A Shape layer with Effects.bHasStroke=true (lfx2 stroke on a shape, NOT cleared by D-03 because vstk did not run/win) ALSO produces the same _Stroke sibling — STROKE-01 requirement covers Image AND Shape layers per REQUIREMENTS.md wording 'Image/shape layers with lfx2 bHasStroke'"
    - "A Shape layer with Effects.bHasVectorStroke=true on a canvas parent produces the same _Stroke sibling using Effects.VectorStrokeSize and Effects.VectorStrokeColor in place of the lfx2 fields (STROKE-03)"
    - "When both Effects.bHasStroke and Effects.bHasVectorStroke would be set on the same Shape layer, the parser already cleared bHasStroke in plan 22-01 (D-03 vstk-wins guard), so the generator sees AT MOST one of the two flags — only ONE _Stroke sibling is ever emitted per Shape layer"
    - "Non-canvas parents (UVerticalBox, UHorizontalBox, etc.) do not emit a _Stroke sibling; a UE_LOG Warning is written and the import continues (mirrors FX-04 drop-shadow non-canvas behaviour)"
    - "The existing FX-05 'Flatten fallback' comment in FWidgetBlueprintGenerator.cpp (line 107) is unchanged; the new block is labelled FX-06 (RESEARCH Pitfall 5)"
  artifacts:
    - path: "Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp"
      provides: "FX-06 stroke sibling block inside the canvas-only section of PopulateChildren, reading bHasStroke (Image OR Shape) OR bHasVectorStroke (Shape only) and emitting a sized + offset UImage at ZOrder = main - 1"
      contains: "FX-06: Stroke sibling"
    - path: "Source/PSD2UMG/Private/Mapper/FShapeLayerMapper.cpp"
      provides: "Updated header comment removing the obsolete 'Future stroke rendering will attach here' line and pointing readers to FWidgetBlueprintGenerator FX-06 block per D-01"
      contains: "FX-06"
    - path: "Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp"
      provides: "Three new It() blocks under FWidgetBlueprintGenSpec::Define — STROKE-01 image lfx2, STROKE-01 shape lfx2 (residual after D-03), STROKE-03 shape vstk; plus a non-canvas parent warning assertion"
      contains: "_Stroke"
  key_links:
    - from: "PopulateChildren canvas-only block (after FX-04 drop shadow ~line 366)"
      to: "FX-06 stroke sibling emit"
      via: "combined condition bImageStroke || bShapeStroke (where bImageStroke covers BOTH Image AND Shape layers carrying lfx2 bHasStroke per STROKE-01)"
      pattern: "FX-06: Stroke sibling"
    - from: "FX-06 block"
      to: "UCanvasPanelSlot::SetZOrder"
      via: "main ZOrder minus one (mirrors FX-04 drop shadow)"
      pattern: "SetZOrder\\(CanvasSlot->GetZOrder\\(\\)\\s*-\\s*1\\)"
    - from: "FX-06 block"
      to: "FPsdLayerEffects.bHasVectorStroke"
      via: "Shape-layer condition (STROKE-03)"
      pattern: "Effects\\.bHasVectorStroke\\s*&&"
    - from: "FX-06 block"
      to: "FPsdLayerEffects.bHasStroke"
      via: "Image-OR-Shape-layer condition (STROKE-01 — Image and Shape layers both qualify)"
      pattern: "Effects\\.bHasStroke\\s*&&"
    - from: "FX-06 block — Pitfall 6 offset expansion"
      to: "UCanvasPanelSlot Offsets.Right and Offsets.Bottom"
      via: "non-stretch canvas slot encoding (Right=Width, Bottom=Height) requires expansion alongside Left/Top decrement"
      pattern: "StrokeOffsets\\.(Right|Bottom)\\s*\\+=\\s*2\\.f\\s*\\*\\s*StrokePx"
    - from: "STROKE-01 Image-OR-Shape condition"
      to: "EPsdLayerType::Image AND EPsdLayerType::Shape inclusion"
      via: "type guard accepts both per REQUIREMENTS.md 'Image/shape layers with lfx2 bHasStroke'"
      pattern: "EPsdLayerType::Image\\s*\\|\\|\\s*LayerPtr->Type\\s*==\\s*EPsdLayerType::Shape"
---

<objective>
STROKE-01 and STROKE-03: Add a single FX-06 stroke-sibling block inside the canvas-only
section of `FWidgetBlueprintGenerator::PopulateChildren` that emits an extra UImage sibling
whenever either:
- an Image OR Shape layer carries lfx2 `bHasStroke=true` (STROKE-01 — the requirement covers
  both Image and Shape layers per REQUIREMENTS.md wording "Image/shape layers with lfx2
  bHasStroke set"; a Shape layer with bHasStroke=true and no vstk block retains the lfx2
  flag because D-03 only clears bHasStroke when bHasVectorStroke is set), or
- a Shape layer carries vstk `bHasVectorStroke=true` (STROKE-03 — populated by plan 22-01).

Implementation strategy is locked by 22-CONTEXT D-01 (sibling lives in the generator, NOT in
FShapeLayerMapper), D-02 (always sibling UImage with NoDrawType brush — never DrawType::Border),
D-03 (parser already cleared bHasStroke on Shape layers when bHasVectorStroke was set, so a
Shape layer reaching the generator with bHasStroke=true cannot also have bHasVectorStroke=true
— the OR never double-emits per layer), and 22-RESEARCH Pattern 4 + Pitfall 5 (block label is
FX-06 because line 107 already uses FX-05) + Pitfall 6 (canvas slot Offsets.Right/Bottom
encode Width/Height in non-stretch mode and MUST be expanded by 2*StrokePx, not just
decremented on Left/Top).

Three new generator specs assert the sibling shape:
- `should create stroke sibling for image layers with lfx2 stroke` (STROKE-01 — Image)
- `should create stroke sibling for shape layers with lfx2 stroke` (STROKE-01 — Shape, residual lfx2-on-Shape case)
- `should create stroke sibling for shape layers with vstk vector stroke` (STROKE-03)
- `should warn and emit no sibling for stroke on non-canvas parent` (defensive — mirrors the
  existing GroupShadowNonCanvas test).

Purpose: Close STROKE-01 (Image AND Shape lfx2 paths) and STROKE-03 (Shape vstk path).
Phase 22 success criteria 2 and 3 (image lfx2 stroke emits a sibling; shape vstk stroke emits
the same shape sibling without double-emit) become empirically satisfiable via automation.
The Shape-with-lfx2-only residual path (no vstk block, lfx2 bHasStroke set) is also covered
to satisfy STROKE-01's full requirement scope.

Output: PopulateChildren has a new FX-06 block; FShapeLayerMapper.cpp header comment is
updated to point at the generator (D-01); FWidgetBlueprintGenSpec.cpp has four new It() blocks
exercising the stroke sibling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/22-stroke-rendering/22-CONTEXT.md
@.planning/phases/22-stroke-rendering/22-RESEARCH.md
@.planning/phases/22-stroke-rendering/22-VALIDATION.md
@.planning/phases/22-stroke-rendering/22-01-PLAN.md
@Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp
@Source/PSD2UMG/Private/Mapper/FShapeLayerMapper.cpp
@Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp

<interfaces>
Key contracts the executor must respect. Extracted from current source at HEAD.

From Source/PSD2UMG/Public/Parser/PsdTypes.h (after plan 22-01 lands — these fields ARE
guaranteed to exist by the time this plan runs because depends_on=[22-01]):

  // Phase 4.1 -- existing lfx2 stroke (Image AND Shape layers; Phase 22 emits for both per STROKE-01)
  bool bHasStroke = false;
  FLinearColor StrokeColor = FLinearColor::Transparent;
  float StrokeSize = 0.f;
  // Phase 22 STROKE-02 -- new vstk vector stroke (Shape layers; consumed here for STROKE-03)
  bool bHasVectorStroke = false;
  float VectorStrokeSize = 0.f;
  FLinearColor VectorStrokeColor = FLinearColor::Transparent;

From Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp FX-04 drop shadow block
(lines 305-366 — DIRECT MODEL for FX-06 stroke sibling): canvas-only-guarded block that
constructs a UImage via Tree->ConstructWidget, names it via MakeUniqueObjectName with
"%s_Shadow" template, builds an FSlateBrush with NoDrawType for Group layers / texture-copy
for Image layers, attaches via CanvasParent->AddChildToCanvas, copies Anchors + Offsets from
CanvasSlot->GetLayout(), shifts Offsets.Left/Top by DropShadowOffset, sets ZOrder =
CanvasSlot->GetZOrder() - 1.

From Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp line 107:
  // FX-05: Flatten fallback — if layer has complex effects and setting enabled, ...

This comment MUST NOT be edited. The new stroke block uses label `// FX-06: Stroke sibling`.

From Source/PSD2UMG/Private/Mapper/FShapeLayerMapper.cpp lines 11-13 (header comment to update):

  // Semantically distinct from FSolidFillLayerMapper (D-03): SolidFill =
  // AdjustmentLayer with SoCo tagged block (a fill layer). Shape = ShapeLayer
  // (Rectangle/Ellipse/Pen tool) with vscg tagged block carrying
  // Type==solidColorLayer (a drawn vector shape). Future stroke rendering
  // (vstk -> UMG border/outline) will attach here, not in FSolidFillLayerMapper.

The "Future stroke rendering ... will attach here" sentence is now obsolete (D-01 placed the
sibling in the generator). Update per PART D below.

From Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp drop-shadow test pattern (lines 260-299
— MODEL for STROKE-01 spec): build FPsdDocument with one Image layer carrying bHasDropShadow,
call GenerateTracked, retrieve UCanvasPanel root, assert children count = 2, identify shadow
sibling by Cast<UImage>, assert ZOrder math.

From Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp group-shadow non-canvas test (lines
831-880 — MODEL for non-canvas-parent stroke warning spec): build outer @vbox group with
inner Group/Image carrying bHasDropShadow, assert no sibling appears.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FX-06 stroke sibling block to FWidgetBlueprintGenerator + update FShapeLayerMapper header comment</name>
  <files>Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp, Source/PSD2UMG/Private/Mapper/FShapeLayerMapper.cpp</files>
  <read_first>
- Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp (lines 105-130 — context around the existing `// FX-05: Flatten fallback` comment, do not edit; lines 305-374 — FX-04 drop-shadow block is the direct structural model for FX-06; lines 376-385 — non-canvas warning placement for shadow, mirror for stroke)
- Source/PSD2UMG/Private/Mapper/FShapeLayerMapper.cpp (lines 1-14 — header comment containing the obsolete "Future stroke rendering will attach here" line)
- .planning/phases/22-stroke-rendering/22-RESEARCH.md (Pattern 4 — generator skeleton; Pitfall 5 — FX label conflict; Pitfall 6 — Offsets.Right/Bottom expansion)
- .planning/phases/22-stroke-rendering/22-CONTEXT.md (D-01, D-02, D-03 — locked decisions on where the sibling lives and how it's drawn)
- .planning/phases/22-stroke-rendering/22-01-PLAN.md (key_links and must_haves for the new FPsdLayerEffects fields the generator consumes)
  </read_first>
  <behavior>
- Test 1 (Task 2 of this plan): Image layer with Effects.bHasStroke=true and Effects.StrokeSize=4 produces a UCanvasPanel root with TWO children — main UImage and a sibling UImage whose name contains "_Stroke" — at ZOrder differing by exactly 1.
- Test 2 (Task 2): Shape layer with Effects.bHasStroke=true (lfx2 only — no vstk block, so bHasVectorStroke=false) produces the same two-child structure (STROKE-01 Image-OR-Shape coverage; the residual lfx2-on-Shape case the parser does NOT clear because D-03 only fires when vstk wins).
- Test 3 (Task 2): Shape layer with Effects.bHasVectorStroke=true and Effects.VectorStrokeSize=3 produces the same two-child structure (main UImage from FShapeLayerMapper + _Stroke sibling).
- Test 4 (Task 2): Image or Shape layer with stroke set inside a non-canvas parent (UVerticalBox) produces NO _Stroke sibling and emits a UE_LOG Warning (parity with FX-04 drop-shadow non-canvas branch).
  </behavior>
  <action>
PART A — Insert the FX-06 stroke sibling block in
Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp.

Insertion point: immediately AFTER the closing `}` of the FX-04 drop-shadow block at line
~366 (`} // end canvas-only block` is line 374 — insert BEFORE that closing brace; insertion
must be inside the `if (UCanvasPanelSlot* CanvasSlot = Cast<UCanvasPanelSlot>(Slot))` block,
AFTER the FX-04 drop-shadow `if (LayerPtr->Effects.bHasDropShadow && bShadowSupportedType)`
construct AND its `else if (LayerPtr->Effects.bHasDropShadow)` warning, BEFORE the closing
`}` of the canvas-only block).

Insert verbatim (do not rename variables; do not refactor into a helper unless the executor
also tightens the test pattern accordingly):

```
// FX-06: Stroke sibling — canvas-only sibling UImage (STROKE-01 / STROKE-03)
// STROKE-01: Image OR Shape layers with lfx2 bHasStroke (populated by ExtractLfx2Stroke
//            since Phase 4.1). REQUIREMENTS.md says "Image/shape layers with lfx2 bHasStroke"
//            so BOTH EPsdLayerType::Image AND EPsdLayerType::Shape qualify under this branch.
//            A Shape layer reaches this branch with bHasStroke=true ONLY when no vstk block
//            was found (D-03 only clears bHasStroke when bHasVectorStroke is set), so the
//            STROKE-01 lfx2-on-Shape residual path is correctly handled here.
// STROKE-03: Shape layers with vstk bHasVectorStroke (populated by ScanVstkStroke in Plan 22-01).
// D-02: always a sibling UImage with NoDrawType brush — no DrawType::Border, no texture.
// D-03: parser cleared bHasStroke on Shape layers when bHasVectorStroke was set, so the two
//       conditions are mutually exclusive PER SHAPE LAYER — the OR never double-emits.
// Pitfall 5: this block is FX-06; line 107's // FX-05: Flatten fallback comment is unchanged.
// Pitfall 6: in non-stretch canvas slots Offsets.Right and Offsets.Bottom encode Width
//            and Height — both must be expanded by 2*StrokePx in addition to shifting
//            Offsets.Left and Offsets.Top by -StrokePx, otherwise the sibling renders at
//            the original size, displaced.
const bool bImageStroke = LayerPtr->Effects.bHasStroke
    && (LayerPtr->Type == EPsdLayerType::Image
        || LayerPtr->Type == EPsdLayerType::Shape);
const bool bShapeStroke = LayerPtr->Effects.bHasVectorStroke
    && LayerPtr->Type == EPsdLayerType::Shape;

if (bImageStroke || bShapeStroke)
{
    // bShapeStroke (vstk) takes precedence over bImageStroke (lfx2) when both
    // happen to be set on the same Shape layer. In practice the parser's D-03
    // guard already cleared bHasStroke when bHasVectorStroke was set, so on a
    // real Shape layer at most one path is true. This conditional is a defence
    // -in-depth guarantee that ONE sibling is emitted per layer regardless.
    const FLinearColor StrokeColor = bShapeStroke
        ? LayerPtr->Effects.VectorStrokeColor
        : LayerPtr->Effects.StrokeColor;
    const float StrokePx = bShapeStroke
        ? LayerPtr->Effects.VectorStrokeSize
        : LayerPtr->Effects.StrokeSize;

    // Skip degenerate strokes (zero width or fully transparent — nothing to render).
    if (StrokePx > 0.f && StrokeColor.A > 0.f)
    {
        const FString StrokeBaseName = !LayerPtr->ParsedTags.CleanName.IsEmpty()
            ? LayerPtr->ParsedTags.CleanName
            : LayerPtr->Name;
        const FName StrokeFName = MakeUniqueObjectName(
            Tree, UImage::StaticClass(),
            FName(*FString::Printf(TEXT("%s_Stroke"), *StrokeBaseName)));
        UImage* StrokeImg = Tree->ConstructWidget<UImage>(UImage::StaticClass(), StrokeFName);

        FSlateBrush StrokeBrush;
        StrokeBrush.DrawAs = ESlateBrushDrawType::NoDrawType; // D-02: no texture
        StrokeBrush.TintColor = FSlateColor(StrokeColor);
        StrokeBrush.ImageSize = FVector2D(
            static_cast<float>(LayerPtr->Bounds.Width())  + 2.f * StrokePx,
            static_cast<float>(LayerPtr->Bounds.Height()) + 2.f * StrokePx);
        StrokeImg->SetBrush(StrokeBrush);
        StrokeImg->SetRenderOpacity(StrokeColor.A);

        UCanvasPanelSlot* StrokeSlot = CanvasParent->AddChildToCanvas(StrokeImg);
        if (StrokeSlot)
        {
            FAnchorData StrokeData;
            StrokeData.Anchors = CanvasSlot->GetLayout().Anchors;
            FMargin StrokeOffsets = CanvasSlot->GetLayout().Offsets;
            // Pitfall 6: shift Left/Top AND expand Right/Bottom (which encode W/H in non-stretch).
            StrokeOffsets.Left   -= StrokePx;
            StrokeOffsets.Top    -= StrokePx;
            StrokeOffsets.Right  += 2.f * StrokePx;
            StrokeOffsets.Bottom += 2.f * StrokePx;
            StrokeData.Offsets = StrokeOffsets;
            StrokeData.Alignment = CanvasSlot->GetLayout().Alignment;
            StrokeSlot->SetLayout(StrokeData);
            // ZOrder = main - 1 (behind main widget; mirrors FX-04 drop shadow)
            StrokeSlot->SetZOrder(CanvasSlot->GetZOrder() - 1);

            UE_LOG(LogPSD2UMG, Log,
                TEXT("FX-06 stroke sibling for layer '%s' (type=%d): StrokePx=%.2f tint=(%.2f,%.2f,%.2f,%.2f) ZOrder=%d"),
                *LayerPtr->Name, static_cast<int32>(LayerPtr->Type),
                StrokePx, StrokeColor.R, StrokeColor.G, StrokeColor.B, StrokeColor.A,
                StrokeSlot->GetZOrder());
        }
    }
    else
    {
        UE_LOG(LogPSD2UMG, Verbose,
            TEXT("FX-06 stroke skipped for layer '%s': degenerate StrokePx=%.2f StrokeColor.A=%.2f"),
            *LayerPtr->Name, StrokePx, StrokeColor.A);
    }
}
```

PART B — Add the non-canvas-parent warning right after the closing `}` of the canvas-only
block (mirrors the existing drop-shadow warning at lines 376-385). Insertion point: after
line 385 `}` of the existing drop-shadow non-canvas warning, BEFORE `// ---- Phase 5: Layer
Effects ...` at line 387.

Insert verbatim:

```
// ---- FX-06 stroke skip warning for non-canvas parents (mirror of FX-04 D-03 / Pitfall 4) ----
// STROKE-01/STROKE-03: stroke sibling pattern is canvas-only. Inside a non-canvas parent
// (UVerticalBox, UHorizontalBox, UScrollBox, etc.) we cannot place a free-floating sibling
// at a sized + offset slot, so we emit a warning and skip — mirroring the FX-04 drop-shadow
// non-canvas behaviour.
if (!CanvasParent)
{
    const bool bImageStrokeNoCanvas = LayerPtr->Effects.bHasStroke
        && (LayerPtr->Type == EPsdLayerType::Image
            || LayerPtr->Type == EPsdLayerType::Shape);
    const bool bShapeStrokeNoCanvas = LayerPtr->Effects.bHasVectorStroke
        && LayerPtr->Type == EPsdLayerType::Shape;
    if (bImageStrokeNoCanvas || bShapeStrokeNoCanvas)
    {
        UE_LOG(LogPSD2UMG, Warning,
            TEXT("Stroke on %s layer '%s' inside non-canvas parent '%s' — no-op (canvas-only sibling pattern)."),
            bShapeStrokeNoCanvas ? TEXT("shape-vstk") : TEXT("image-or-shape-lfx2"),
            *LayerPtr->Name, *Parent->GetName());
    }
}
```

PART C — Update the obsolete header comment in
Source/PSD2UMG/Private/Mapper/FShapeLayerMapper.cpp lines 11-13.

Replace the sentence:
```
// Type==solidColorLayer (a drawn vector shape). Future stroke rendering
// (vstk -> UMG border/outline) will attach here, not in FSolidFillLayerMapper.
```
with:
```
// Type==solidColorLayer (a drawn vector shape). Stroke rendering (vstk) is
// emitted by FWidgetBlueprintGenerator's FX-06 stroke-sibling block (Phase 22
// STROKE-03, D-01) — NOT in this mapper.
```

DO NOT change the FShapeLayerMapper class body, priority, or Map() implementation.

PART D — Verify the existing FX-05 comment at FWidgetBlueprintGenerator.cpp line 107 is
unchanged. The line must still read:
```
// FX-05: Flatten fallback — if layer has complex effects and setting enabled,
```
If editing accidentally moved this line, restore it. The new stroke block is `// FX-06: Stroke sibling`.
  </action>
  <verify>
    <automated>cd "C:/Dev/psd-to-umg" &amp;&amp; grep -c "FX-06: Stroke sibling" Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp &amp;&amp; grep -c "FX-05: Flatten fallback" Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp &amp;&amp; grep -c "FX-06" Source/PSD2UMG/Private/Mapper/FShapeLayerMapper.cpp &amp;&amp; grep -c "StrokeOffsets\.Right  += 2\.f \* StrokePx" Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp &amp;&amp; grep -c "StrokeOffsets\.Bottom += 2\.f \* StrokePx" Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp &amp;&amp; grep -c "EPsdLayerType::Image" Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp &amp;&amp; grep -c "LayerPtr->Type == EPsdLayerType::Shape" Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp</automated>
  </verify>
  <acceptance_criteria>
- File `Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` contains the literal comment `// FX-06: Stroke sibling` (NOT `FX-05` for the new block)
- File `Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` STILL contains the literal comment `// FX-05: Flatten fallback` (unchanged from line 107 — not removed, not renumbered)
- File `Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` contains BOTH `EPsdLayerType::Image` AND `LayerPtr->Type == EPsdLayerType::Shape` inside the FX-06 bImageStroke condition (STROKE-01 covers Image OR Shape per REQUIREMENTS.md)
- File `Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` contains the literal expression `Effects.bHasStroke` AND `Effects.bHasVectorStroke` inside the FX-06 block
- File `Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` contains the offset arithmetic `StrokeOffsets.Right  += 2.f * StrokePx;` AND `StrokeOffsets.Bottom += 2.f * StrokePx;` (Pitfall 6 — both expansions present, NOT just Left/Top decrements; key_links pattern enforces this)
- File `Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` contains the literal expression `CanvasSlot->GetZOrder() - 1` inside the FX-06 block (ZOrder = main - 1, mirroring FX-04)
- File `Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` contains the literal `_Stroke` substring (used in the FString::Printf widget name template)
- File `Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` contains the warning string literal `Stroke on %s layer '%s' inside non-canvas parent` (the non-canvas guard)
- File `Source/PSD2UMG/Private/Mapper/FShapeLayerMapper.cpp` contains the substring `FX-06` in its header comment
- File `Source/PSD2UMG/Private/Mapper/FShapeLayerMapper.cpp` does NOT contain the obsolete sentence `Future stroke rendering` (removed in PART C)
- `grep -c "FX-06: Stroke sibling" Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` outputs `1` or higher
- `grep -c "FX-05: Flatten fallback" Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` outputs `1` or higher
- `grep -c "FX-06" Source/PSD2UMG/Private/Mapper/FShapeLayerMapper.cpp` outputs `1` or higher
- `grep -c "StrokeOffsets\.Right  += 2\.f \* StrokePx" Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` outputs `1` or higher (Pitfall 6 Right expansion)
- `grep -c "StrokeOffsets\.Bottom += 2\.f \* StrokePx" Source/PSD2UMG/Private/Generator/FWidgetBlueprintGenerator.cpp` outputs `1` or higher (Pitfall 6 Bottom expansion)
- The plugin compiles after these edits (build output contains `Build Successful` or equivalent — defer to executor's build step)
  </acceptance_criteria>
  <done>
FWidgetBlueprintGenerator.cpp has a new FX-06 block inside the canvas-only section of
PopulateChildren that emits a sibling UImage for: (a) Image-with-bHasStroke layers, (b)
Shape-with-bHasStroke layers (the lfx2-on-Shape residual case STROKE-01 covers but D-03 does
not clear), and (c) Shape-with-bHasVectorStroke layers. Sibling sizing applies +2*StrokePx to
both ImageSize and canvas-slot Offsets.Right/Bottom (Pitfall 6); offsets shift Left/Top by
-StrokePx; ZOrder is main - 1. A non-canvas warning is emitted for any of the three stroke
cases inside non-canvas parents. FShapeLayerMapper.cpp header comment now points readers to
the generator's FX-06 block. The existing FX-05 'Flatten fallback' comment is unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add four new It() blocks to FWidgetBlueprintGenSpec exercising STROKE-01 (Image), STROKE-01 (Shape lfx2 residual), STROKE-03, and the non-canvas stroke warning</name>
  <files>Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp</files>
  <read_first>
- Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp (lines 25-62 — BEGIN_DEFINE_SPEC and helper GenerateTracked / CleanupAsset, including the Pitfall 5 ParsedTags population in GenerateTracked; lines 260-299 — drop-shadow It() block as model for STROKE-01; lines 758-829 — group-shadow It() block as model for STROKE-03; lines 831-880 — non-canvas drop-shadow warning It() block as model for the non-canvas stroke warning)
- Source/PSD2UMG/Tests/TestHelpers.h (PopulateParsedTagsForDocument helper signature so the new tests stay consistent with existing GenerateTracked usage)
- .planning/phases/22-stroke-rendering/22-VALIDATION.md (Wave 0 line 53: "FWidgetBlueprintGenSpec.cpp — add StrokeSibling spec group for STROKE-01/STROKE-03 generator assertions")
  </read_first>
  <behavior>
- Test 1 (STROKE-01 Image): A document with one canvas-rooted Image layer carrying Effects.bHasStroke=true, Effects.StrokeSize=4, Effects.StrokeColor=red produces a generated WBP whose root UCanvasPanel has exactly 2 children — one main UImage and one sibling UImage whose name contains "_Stroke", at ZOrders differing by exactly 1 (sibling = main - 1).
- Test 2 (STROKE-01 Shape lfx2 residual): A document with one canvas-rooted Shape layer carrying Effects.bHasStroke=true, Effects.StrokeSize=2, Effects.StrokeColor=green AND Effects.bHasVectorStroke=false (no vstk parsed; lfx2 only) AND Effects.bHasColorOverlay=true produces the same two-child shape. This proves STROKE-01's "Image/shape layers with lfx2 bHasStroke" coverage — the residual case D-03 does NOT clear (because vstk did not win).
- Test 3 (STROKE-03): A document with one canvas-rooted Shape layer carrying Effects.bHasVectorStroke=true, Effects.VectorStrokeSize=3, Effects.VectorStrokeColor=blue, AND Effects.bHasColorOverlay=true (so FShapeLayerMapper produces its UImage cleanly) produces the same two-child shape.
- Test 4 (non-canvas guard): An Image layer carrying Effects.bHasStroke=true placed inside a non-canvas Group (mapped via @vbox tag to UVerticalBox) produces NO _Stroke sibling — verified by walking the WBP tree and asserting zero UImage widgets have a name containing "_Stroke".
  </behavior>
  <action>
Add FOUR new It() blocks to Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp inside the
existing `Describe("FWidgetBlueprintGenerator::Generate", ...)` lambda at line 66.

Insertion point for all four: AFTER the existing drop-shadow It() block (which ends at line
299 `});`) and BEFORE the `It("should assign ZOrder inversely from layer index", ...)` block
at line 301. Place all four new blocks adjacent to each other for cohesion.

DO NOT modify the BeforeEach, AfterEach, GenerateTracked, or CleanupAsset functions. The new
tests use the existing infrastructure verbatim.

Insert verbatim:

```
It("should create stroke sibling for image layers with lfx2 stroke (STROKE-01 Image)", [this]()
{
    FPsdDocument Doc;
    Doc.CanvasSize = FIntPoint(400, 400);
    Doc.SourcePath = TEXT("C:/test/ImageStroke.psd");

    FPsdLayer& Layer = Doc.RootLayers.AddDefaulted_GetRef();
    Layer.Name = TEXT("StrokedImage");
    Layer.Type = EPsdLayerType::Image;
    Layer.Bounds = FIntRect(50, 50, 150, 150);
    Layer.PixelWidth = 100;
    Layer.PixelHeight = 100;
    Layer.RGBAPixels.SetNumZeroed(100 * 100 * 4);
    Layer.Effects.bHasStroke = true;
    Layer.Effects.StrokeSize = 4.f;
    Layer.Effects.StrokeColor = FLinearColor(1.f, 0.f, 0.f, 1.f); // red

    UWidgetBlueprint* WBP = GenerateTracked(Doc, TEXT("/Game/_Test/WBPGen"), TEXT("WBP_ImageStroke"));
    TestNotNull(TEXT("WBP created"), WBP);
    if (!WBP) return;

    UCanvasPanel* Root = Cast<UCanvasPanel>(WBP->WidgetTree->RootWidget);
    TestNotNull(TEXT("Root is UCanvasPanel"), Root);
    if (!Root) return;

    TestEqual(TEXT("Canvas has main + stroke sibling (2 children)"), Root->GetChildrenCount(), 2);
    if (Root->GetChildrenCount() != 2) return;

    // Identify sibling by name suffix
    UImage* StrokeImg = nullptr;
    UImage* MainImg = nullptr;
    for (int32 c = 0; c < Root->GetChildrenCount(); ++c)
    {
        if (UImage* AsImg = Cast<UImage>(Root->GetChildAt(c)))
        {
            if (AsImg->GetName().Contains(TEXT("_Stroke"))) StrokeImg = AsImg;
            else MainImg = AsImg;
        }
    }
    TestNotNull(TEXT("Stroke UImage exists"), StrokeImg);
    TestNotNull(TEXT("Main UImage exists"), MainImg);
    if (!StrokeImg || !MainImg) return;

    UCanvasPanelSlot* StrokeSlot = Cast<UCanvasPanelSlot>(StrokeImg->Slot);
    UCanvasPanelSlot* MainSlot   = Cast<UCanvasPanelSlot>(MainImg->Slot);
    TestNotNull(TEXT("StrokeSlot valid"), StrokeSlot);
    TestNotNull(TEXT("MainSlot valid"), MainSlot);
    if (StrokeSlot && MainSlot)
    {
        TestEqual(TEXT("Stroke ZOrder = main ZOrder - 1"),
            StrokeSlot->GetZOrder(), MainSlot->GetZOrder() - 1);

        // Pitfall 6: stroke offsets are shifted Left/Top by -StrokePx and Right/Bottom by +2*StrokePx
        FMargin MainOffsets   = MainSlot->GetLayout().Offsets;
        FMargin StrokeOffsets = StrokeSlot->GetLayout().Offsets;
        TestTrue(TEXT("Stroke Left = main Left - 4"),
            FMath::IsNearlyEqual(StrokeOffsets.Left, MainOffsets.Left - 4.f, 0.01f));
        TestTrue(TEXT("Stroke Top = main Top - 4"),
            FMath::IsNearlyEqual(StrokeOffsets.Top, MainOffsets.Top - 4.f, 0.01f));
        TestTrue(TEXT("Stroke Right = main Right + 8"),
            FMath::IsNearlyEqual(StrokeOffsets.Right, MainOffsets.Right + 8.f, 0.01f));
        TestTrue(TEXT("Stroke Bottom = main Bottom + 8"),
            FMath::IsNearlyEqual(StrokeOffsets.Bottom, MainOffsets.Bottom + 8.f, 0.01f));
    }

    // Tint matches StrokeColor
    FSlateBrush StrokeBrush = StrokeImg->GetBrush();
    FLinearColor Tint = StrokeBrush.TintColor.GetSpecifiedColor();
    TestTrue(TEXT("Stroke tint R near 1"), FMath::IsNearlyEqual(Tint.R, 1.f, 0.01f));
    TestTrue(TEXT("Stroke tint G near 0"), FMath::IsNearlyEqual(Tint.G, 0.f, 0.01f));
    TestTrue(TEXT("Stroke tint B near 0"), FMath::IsNearlyEqual(Tint.B, 0.f, 0.01f));
    TestEqual(TEXT("Stroke brush DrawAs is NoDrawType"),
        (int32)StrokeBrush.DrawAs, (int32)ESlateBrushDrawType::NoDrawType);
});

It("should create stroke sibling for shape layers with lfx2 stroke (STROKE-01 Shape residual)", [this]()
{
    // STROKE-01 covers BOTH Image AND Shape layers carrying lfx2 bHasStroke.
    // A Shape layer reaches the generator with bHasStroke=true ONLY when no vstk
    // block was found (D-03 in plan 22-01 only clears bHasStroke when bHasVectorStroke
    // is set). This synthetic test exercises that residual path: bHasStroke=true,
    // bHasVectorStroke=false, Type=Shape -> generator MUST emit a _Stroke sibling.
    // Without this case the bImageStroke condition would silently drop Shape layers
    // with lfx2-only stroke (gap flagged by checker in original revision).
    FPsdDocument Doc;
    Doc.CanvasSize = FIntPoint(400, 400);
    Doc.SourcePath = TEXT("C:/test/ShapeLfx2Stroke.psd");

    FPsdLayer& Layer = Doc.RootLayers.AddDefaulted_GetRef();
    Layer.Name = TEXT("StrokedShapeLfx2");
    Layer.Type = EPsdLayerType::Shape;
    Layer.Bounds = FIntRect(80, 80, 200, 200);
    Layer.bVisible = true;
    // FShapeLayerMapper produces a zero-texture UImage tinted by FX-03 from ColorOverlayColor;
    // populate that so the main widget renders cleanly.
    Layer.Effects.bHasColorOverlay = true;
    Layer.Effects.ColorOverlayColor = FLinearColor(0.5f, 0.5f, 0.5f, 1.f);
    // lfx2 stroke (STROKE-01 — Shape residual case; vstk did NOT win, so bHasStroke remains)
    Layer.Effects.bHasStroke = true;
    Layer.Effects.StrokeSize = 2.f;
    Layer.Effects.StrokeColor = FLinearColor(0.f, 1.f, 0.f, 1.f); // green
    Layer.Effects.bHasVectorStroke = false; // explicit: no vstk parsed
    Layer.Effects.VectorStrokeSize = 0.f;

    UWidgetBlueprint* WBP = GenerateTracked(Doc, TEXT("/Game/_Test/WBPGen"), TEXT("WBP_ShapeLfx2Stroke"));
    TestNotNull(TEXT("WBP created"), WBP);
    if (!WBP) return;

    UCanvasPanel* Root = Cast<UCanvasPanel>(WBP->WidgetTree->RootWidget);
    TestNotNull(TEXT("Root is UCanvasPanel"), Root);
    if (!Root) return;

    TestEqual(TEXT("Shape lfx2: canvas has main + stroke sibling (2 children)"),
        Root->GetChildrenCount(), 2);
    if (Root->GetChildrenCount() != 2) return;

    UImage* StrokeImg = nullptr;
    UImage* MainImg = nullptr;
    for (int32 c = 0; c < Root->GetChildrenCount(); ++c)
    {
        if (UImage* AsImg = Cast<UImage>(Root->GetChildAt(c)))
        {
            if (AsImg->GetName().Contains(TEXT("_Stroke"))) StrokeImg = AsImg;
            else MainImg = AsImg;
        }
    }
    TestNotNull(TEXT("Shape lfx2 stroke UImage exists (STROKE-01 residual on Shape)"), StrokeImg);
    TestNotNull(TEXT("Shape lfx2 main UImage exists"), MainImg);
    if (!StrokeImg || !MainImg) return;

    UCanvasPanelSlot* StrokeSlot = Cast<UCanvasPanelSlot>(StrokeImg->Slot);
    UCanvasPanelSlot* MainSlot   = Cast<UCanvasPanelSlot>(MainImg->Slot);
    if (StrokeSlot && MainSlot)
    {
        TestEqual(TEXT("Shape lfx2 stroke ZOrder = main - 1"),
            StrokeSlot->GetZOrder(), MainSlot->GetZOrder() - 1);

        // Pitfall 6 verification on the lfx2-on-Shape path
        FMargin MainOffsets   = MainSlot->GetLayout().Offsets;
        FMargin StrokeOffsets = StrokeSlot->GetLayout().Offsets;
        TestTrue(TEXT("Shape lfx2 stroke Left = main Left - 2"),
            FMath::IsNearlyEqual(StrokeOffsets.Left, MainOffsets.Left - 2.f, 0.01f));
        TestTrue(TEXT("Shape lfx2 stroke Right = main Right + 4"),
            FMath::IsNearlyEqual(StrokeOffsets.Right, MainOffsets.Right + 4.f, 0.01f));
    }

    // Tint must come from lfx2 StrokeColor (green), not VectorStrokeColor (zero)
    FSlateBrush StrokeBrush = StrokeImg->GetBrush();
    FLinearColor Tint = StrokeBrush.TintColor.GetSpecifiedColor();
    TestTrue(TEXT("Shape lfx2 stroke tint R near 0"), FMath::IsNearlyEqual(Tint.R, 0.f, 0.01f));
    TestTrue(TEXT("Shape lfx2 stroke tint G near 1 (lfx2 StrokeColor wins, not VectorStrokeColor)"),
        FMath::IsNearlyEqual(Tint.G, 1.f, 0.01f));
    TestTrue(TEXT("Shape lfx2 stroke tint B near 0"), FMath::IsNearlyEqual(Tint.B, 0.f, 0.01f));
});

It("should create stroke sibling for shape layers with vstk vector stroke (STROKE-03)", [this]()
{
    FPsdDocument Doc;
    Doc.CanvasSize = FIntPoint(400, 400);
    Doc.SourcePath = TEXT("C:/test/ShapeStroke.psd");

    FPsdLayer& Layer = Doc.RootLayers.AddDefaulted_GetRef();
    Layer.Name = TEXT("StrokedShape");
    Layer.Type = EPsdLayerType::Shape;
    Layer.Bounds = FIntRect(80, 80, 200, 200);
    Layer.bVisible = true;
    // FShapeLayerMapper produces a zero-texture UImage tinted by FX-03 from ColorOverlayColor;
    // populate that so the main widget renders cleanly.
    Layer.Effects.bHasColorOverlay = true;
    Layer.Effects.ColorOverlayColor = FLinearColor(0.5f, 0.5f, 0.5f, 1.f);
    // Vector stroke (STROKE-03)
    Layer.Effects.bHasVectorStroke = true;
    Layer.Effects.VectorStrokeSize = 3.f;
    Layer.Effects.VectorStrokeColor = FLinearColor(0.f, 0.f, 1.f, 1.f); // blue

    UWidgetBlueprint* WBP = GenerateTracked(Doc, TEXT("/Game/_Test/WBPGen"), TEXT("WBP_ShapeStroke"));
    TestNotNull(TEXT("WBP created"), WBP);
    if (!WBP) return;

    UCanvasPanel* Root = Cast<UCanvasPanel>(WBP->WidgetTree->RootWidget);
    TestNotNull(TEXT("Root is UCanvasPanel"), Root);
    if (!Root) return;

    TestEqual(TEXT("Canvas has main + stroke sibling (2 children)"), Root->GetChildrenCount(), 2);
    if (Root->GetChildrenCount() != 2) return;

    UImage* StrokeImg = nullptr;
    UImage* MainImg   = nullptr;
    for (int32 c = 0; c < Root->GetChildrenCount(); ++c)
    {
        if (UImage* AsImg = Cast<UImage>(Root->GetChildAt(c)))
        {
            if (AsImg->GetName().Contains(TEXT("_Stroke"))) StrokeImg = AsImg;
            else MainImg = AsImg;
        }
    }
    TestNotNull(TEXT("Shape stroke UImage exists"), StrokeImg);
    TestNotNull(TEXT("Shape main UImage exists"), MainImg);
    if (!StrokeImg || !MainImg) return;

    UCanvasPanelSlot* StrokeSlot = Cast<UCanvasPanelSlot>(StrokeImg->Slot);
    UCanvasPanelSlot* MainSlot   = Cast<UCanvasPanelSlot>(MainImg->Slot);
    if (StrokeSlot && MainSlot)
    {
        TestEqual(TEXT("Shape stroke ZOrder = main - 1"),
            StrokeSlot->GetZOrder(), MainSlot->GetZOrder() - 1);

        FMargin MainOffsets   = MainSlot->GetLayout().Offsets;
        FMargin StrokeOffsets = StrokeSlot->GetLayout().Offsets;
        TestTrue(TEXT("Shape stroke Left = main Left - 3"),
            FMath::IsNearlyEqual(StrokeOffsets.Left, MainOffsets.Left - 3.f, 0.01f));
        TestTrue(TEXT("Shape stroke Right = main Right + 6"),
            FMath::IsNearlyEqual(StrokeOffsets.Right, MainOffsets.Right + 6.f, 0.01f));
    }

    FSlateBrush StrokeBrush = StrokeImg->GetBrush();
    FLinearColor Tint = StrokeBrush.TintColor.GetSpecifiedColor();
    TestTrue(TEXT("Shape stroke tint R near 0"), FMath::IsNearlyEqual(Tint.R, 0.f, 0.01f));
    TestTrue(TEXT("Shape stroke tint G near 0"), FMath::IsNearlyEqual(Tint.G, 0.f, 0.01f));
    TestTrue(TEXT("Shape stroke tint B near 1"), FMath::IsNearlyEqual(Tint.B, 1.f, 0.01f));
});

It("should NOT create stroke sibling on non-canvas parent (image lfx2 stroke inside @vbox)", [this]()
{
    // Mirrors the FX-04 GroupShadowNonCanvas test (lines 831-880): stroke sibling pattern
    // is canvas-only. Inside a UVerticalBox parent the generator must emit no _Stroke widget.
    FPsdDocument Doc;
    Doc.CanvasSize = FIntPoint(400, 400);
    Doc.SourcePath = TEXT("C:/test/ImageStrokeNonCanvas.psd");

    // Outer @vbox group (maps to UVerticalBox)
    FPsdLayer& OuterLayer = Doc.RootLayers.AddDefaulted_GetRef();
    OuterLayer.Name = TEXT("Container @vbox");
    OuterLayer.Type = EPsdLayerType::Group;
    OuterLayer.Bounds = FIntRect(0, 0, 400, 400);
    OuterLayer.bVisible = true;

    // Inner image with lfx2 stroke
    FPsdLayer& InnerLayer = OuterLayer.Children.AddDefaulted_GetRef();
    InnerLayer.Name = TEXT("InnerStroked");
    InnerLayer.Type = EPsdLayerType::Image;
    InnerLayer.Bounds = FIntRect(10, 10, 100, 100);
    InnerLayer.PixelWidth = 90;
    InnerLayer.PixelHeight = 90;
    InnerLayer.RGBAPixels.SetNumZeroed(90 * 90 * 4);
    InnerLayer.bVisible = true;
    InnerLayer.Effects.bHasStroke = true;
    InnerLayer.Effects.StrokeSize = 2.f;
    InnerLayer.Effects.StrokeColor = FLinearColor(0.f, 1.f, 0.f, 1.f);

    AddExpectedError(TEXT("Stroke on .* layer .* inside non-canvas parent"),
        EAutomationExpectedErrorFlags::Contains, 0);

    UWidgetBlueprint* WBP = GenerateTracked(Doc, TEXT("/Game/_Test/WBPGen"), TEXT("WBP_ImageStrokeNonCanvas"));
    TestNotNull(TEXT("WBP created"), WBP);
    if (!WBP) return;

    // Walk the entire widget tree; assert no widget name contains "_Stroke".
    int32 StrokeWidgetCount = 0;
    WBP->WidgetTree->ForEachWidget([&](UWidget* W)
    {
        if (W && W->GetName().Contains(TEXT("_Stroke"))) ++StrokeWidgetCount;
    });
    TestEqual(TEXT("No _Stroke widget anywhere in tree"), StrokeWidgetCount, 0);
});
```

Note on the AddExpectedError above: the test asserts a UE_LOG Warning is emitted for the
stroke-on-non-canvas case. The pattern matches the warning string emitted in PART B of
Task 1 ("Stroke on %s layer '%s' inside non-canvas parent"). If your UE automation
infrastructure does not surface UE_LOG Warning to test errors (some configurations don't),
remove the AddExpectedError line — the structural assertion (zero `_Stroke` widgets) is the
load-bearing check.

DO NOT add new fixture PSDs. All four tests build FPsdDocument in-memory via
AddDefaulted_GetRef per existing pattern (D-04: positive stroke fixture deferred until
Stroke.psd is provided; these synthetic tests are the spec-level substitute permitted by D-04).
  </action>
  <verify>
    <automated>cd "C:/Dev/psd-to-umg" &amp;&amp; grep -c "STROKE-01 Image" Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp &amp;&amp; grep -c "STROKE-01 Shape residual" Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp &amp;&amp; grep -c "STROKE-03" Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp &amp;&amp; grep -c "_Stroke" Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp</automated>
  </verify>
  <acceptance_criteria>
- File `Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` contains the test description literal `should create stroke sibling for image layers with lfx2 stroke (STROKE-01 Image)`
- File `Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` contains the test description literal `should create stroke sibling for shape layers with lfx2 stroke (STROKE-01 Shape residual)`
- File `Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` contains the test description literal `should create stroke sibling for shape layers with vstk vector stroke (STROKE-03)`
- File `Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` contains the test description literal `should NOT create stroke sibling on non-canvas parent (image lfx2 stroke inside @vbox)`
- File `Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` contains a literal `Layer.Effects.bHasStroke = true;` (used in BOTH STROKE-01 Image and STROKE-01 Shape residual setups)
- File `Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` contains a literal `Layer.Effects.bHasVectorStroke = true;` (STROKE-03 spec setup)
- File `Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` contains the literal `Layer.Effects.bHasVectorStroke = false;` (STROKE-01 Shape residual: explicit non-vstk setup)
- File `Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` contains the substring `_Stroke` AT LEAST 6 times (test description text plus name-suffix matching across the four It() blocks)
- File `Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` contains the literal `MainSlot->GetZOrder() - 1` (ZOrder math assertion across the three positive specs)
- File `Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` contains the literal expression `MainOffsets.Right + 8.f` (Pitfall 6 assertion verifying canvas slot Right was expanded by 2*4=8 in STROKE-01 Image spec)
- File `Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` contains the literal expression `MainOffsets.Right + 4.f` (Pitfall 6 assertion verifying canvas slot Right was expanded by 2*2=4 in STROKE-01 Shape residual spec)
- `grep -c "STROKE-01 Image" Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` outputs `1` or higher
- `grep -c "STROKE-01 Shape residual" Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` outputs `1` or higher
- `grep -c "STROKE-03" Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` outputs `1` or higher
- `grep -c "_Stroke" Source/PSD2UMG/Tests/FWidgetBlueprintGenSpec.cpp` outputs `6` or higher
- The plugin compiles after these edits (build output contains `Build Successful` or equivalent — defer to executor's build step)
- Running automation `PSD2UMG.Generator` in editor — the four new It() blocks (STROKE-01 Image, STROKE-01 Shape residual, STROKE-03 shape, non-canvas warning) all pass; existing It() blocks remain green
  </acceptance_criteria>
  <done>
FWidgetBlueprintGenSpec.cpp has FOUR new It() blocks under the existing Describe lambda
asserting: (1) STROKE-01 Image layer lfx2 stroke produces a sized + offset _Stroke sibling
at ZOrder = main - 1 with red NoDrawType brush, (2) STROKE-01 Shape layer lfx2 stroke
(residual after D-03 — vstk did not win) ALSO produces the sized + offset _Stroke sibling
with green tint sourced from lfx2 StrokeColor (NOT VectorStrokeColor), (3) STROKE-03 Shape
layer vstk stroke produces the same shape with blue tint sourced from VectorStrokeColor,
(4) non-canvas parent (UVerticalBox) suppresses sibling emission and emits a Warning. No
fixture PSDs introduced. No existing tests modified.
  </done>
</task>

</tasks>

<verification>
1. Plugin compiles: `Source/PSD2UMG/PSD2UMG.uplugin` builds cleanly with all three modified files in place.
2. New tests pass: `PSD2UMG.Generator` automation suite — the FOUR new It() blocks (STROKE-01 Image, STROKE-01 Shape residual, STROKE-03 vstk, non-canvas warning) all green.
3. Existing tests unchanged: `should create shadow sibling for drop shadow layers` (line 260), `GRPFX-01 happy path: canvas-parented group with drop shadow` (line 758), `should emit warning for drop shadow on group inside non-canvas parent` (line 831), and all other pre-existing It() blocks still green.
4. Plan 22-01 regression unbroken: `PSD2UMG.Parser.ButtonStyles` still passes (no _Stroke siblings expected from a stroke-free fixture, but that is a parser-level assertion; this plan does not break it).
5. STROKE-01 full-coverage check: the Shape-with-lfx2-only residual case (Test 2) is the regression guard against the requirement-coverage gap originally flagged by the checker — without it, a Shape layer with lfx2 bHasStroke and no vstk block would silently drop its stroke. Test 2 fails LOUDLY if the bImageStroke condition is ever narrowed back to Type==Image only.
6. No double-emit: import a hypothetical PSD layer with BOTH bHasStroke and bHasVectorStroke set in-memory (synthetic test) — only ONE _Stroke sibling is produced. (Note: parser plan 22-01 D-03 guard guarantees this does not happen on real PSDs; the bShapeStroke-takes-precedence ternary in the action body is defence-in-depth at the generator layer.)
7. ButtonStyles.psd visual import: import via `/gsd:execute-phase`-driven test or designer manual import — output WBP renders identically to pre-Phase-22 (no spurious _Stroke widgets).
</verification>

<success_criteria>
- FX-06 stroke sibling block exists in FWidgetBlueprintGenerator.cpp, labelled with `// FX-06: Stroke sibling`, gated on `bImageStroke || bShapeStroke` where bImageStroke covers BOTH Image AND Shape layer types (STROKE-01 full requirement coverage), sizing applies +2*StrokePx to BOTH ImageSize and canvas-slot Offsets.Right/Bottom (Pitfall 6 in must_haves.key_links), ZOrder = main - 1, brush is NoDrawType.
- The pre-existing `// FX-05: Flatten fallback` comment at line 107 is unchanged.
- FShapeLayerMapper.cpp header comment updated; obsolete "Future stroke rendering" sentence removed.
- Four new It() blocks in FWidgetBlueprintGenSpec.cpp: STROKE-01 Image, STROKE-01 Shape residual, STROKE-03, non-canvas guard. All green when run.
- All Phase 21 / Phase 4.1 / Phase 14 / Phase 15 specs (drop shadow, shape, group effects) remain green — the stroke block does not interfere with siblings/ordering of those features.
- `must_haves.truths` line 5 (non-canvas parent emits no sibling and a Warning) verified by Test 4 in Task 2.
- STROKE-01 requirement coverage now empirically complete — Image AND Shape layer paths both exercised by spec.
</success_criteria>

<output>
After completion, create `.planning/phases/22-stroke-rendering/22-02-SUMMARY.md` describing:
- What was built (FX-06 generator block, FShapeLayerMapper comment update, four new It() blocks)
- Decisions confirmed (D-01 generator-side, D-02 NoDrawType always, Pitfall 5 FX label, Pitfall 6 Offsets.Right/Bottom expansion)
- STROKE-01 coverage clarification: Image AND Shape layer paths both exercised; Shape-residual case (lfx2 only, no vstk) covered to satisfy REQUIREMENTS.md "Image/shape layers with lfx2 bHasStroke" wording
- Open items deferred (positive Stroke.psd fixture, inside/outside/center alignment precision, frameFXMulti VlLs stroke emission)
- Anomalies encountered (e.g. AddExpectedError compatibility with the project's automation harness; revert to structural-only assertion if needed)
</output>

---
phase: 17.1-button-variants-state-wiring
plan: 01
subsystem: testing
tags: [automation-spec, button-mapper, variants-mapper, tdd, red-before-green]

# Dependency graph
requires:
  - phase: 17-automated-font-matching
    provides: Phase 17 spec pattern (RED-before-GREEN via Plan 01 failing spec + Plan 02 implementation)
  - phase: 09-unified-layer-naming-convention-tag-based
    provides: FLayerTagParser, @button/@variants/@state:* grammar, TestHelpers.h MakeTaggedTestLayer
  - phase: 03-layer-mapping-widget-blueprint-generation
    provides: FButtonLayerMapper, FVariantsSuffixMapper, IPsdLayerMapper pipeline
provides:
  - FButtonLayerMapperSpec.cpp — automation spec scaffold for BTN-STATE-01 (conflict resolution, RED) and BTN-STATE-02 (state slot wiring)
  - REQUIREMENTS.md BTN-STATE-01 and BTN-STATE-02 entries (pending, assigned to Phase 17.1)
affects:
  - 17.1-02-PLAN.md (Plan 02 turns RED to GREEN by implementing FVariantsSuffixMapper HasType guard + D-03 aggregate warning)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - RED-before-GREEN spec scaffold: spec file created in Plan 01 with intentional failing assertion; implementation added in Plan 02

key-files:
  created:
    - Source/PSD2UMG/Tests/FButtonLayerMapperSpec.cpp
  modified:
    - .planning/REQUIREMENTS.md

key-decisions:
  - "BTN-STATE-01 VariantsMapper.CanMap assertion starts RED: FVariantsSuffixMapper still returns bIsVariants only (no HasType guard). Plan 02 adds the one-line D-01 guard."
  - "BTN-STATE-02 all-four-state and partial-state tests call ButtonMapper.Map directly (no registry round-trip per D-04) to avoid re-sort non-determinism."
  - "PopulateParsedTagsRecursive called on Layer.Children after AddStateChild to satisfy Pitfall 5 (tags not populated on manually-constructed children)."
  - "No TestHasLog assertions per Pitfall 4 — slot population verified via GetResourceObject() != nullptr."

patterns-established:
  - "Phase 17.1 Wave 0 pattern: spec file with intentional RED assertion documents contract before implementation, matching Phase 17 precedent"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-04-22
---

# Phase 17.1 Plan 01: FButtonLayerMapperSpec Wave 0 Scaffold Summary

**RED spec scaffold for @button @variants conflict resolution (BTN-STATE-01) and @state:* child slot wiring (BTN-STATE-02) — Wave 0 test-first contract lock-in before Plan 02 implementation**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-22T15:49:52Z
- **Completed:** 2026-04-22T15:51:45Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Created `FButtonLayerMapperSpec.cpp` with full BTN-STATE-01 (4 It blocks) and BTN-STATE-02 (2 It blocks) spec coverage
- BTN-STATE-01 VariantsMapper.CanMap assertion is intentionally RED: `FVariantsSuffixMapper::CanMap` currently returns `Layer.ParsedTags.bIsVariants` with no `HasType()` guard — the assertion `TestFalse("VariantsMapper rejects typed layer", VariantsMapper.CanMap(L))` fails until Plan 02 adds the one-line D-01 guard
- Added `### Button State Wiring (BTN-STATE-*)` section to REQUIREMENTS.md with both requirements unchecked (Pending) and two new traceability rows, growing the table from 17 to 19 rows
- No production code touched — Plan 01 is test-scaffold only, per established Phase 17 RED-before-GREEN pattern

## Task Commits

Each task was committed atomically:

1. **Task 1: Create FButtonLayerMapperSpec.cpp** - `8f332e1` (test)
2. **Task 2: Add BTN-STATE-01/02 entries to REQUIREMENTS.md** - `2f9c52d` (feat)

**Plan metadata:** (docs commit — see state_updates below)

## Files Created/Modified

- `Source/PSD2UMG/Tests/FButtonLayerMapperSpec.cpp` — New automation spec; 6 It blocks across 2 Describe blocks; spec member state `FButtonLayerMapper ButtonMapper` and `FVariantsSuffixMapper VariantsMapper`; `PopulateParsedTagsRecursive` on child layers; `RGBAPixels.SetNumZeroed(64*64*4)` on each state-image child; slot-population assertions via `GetResourceObject()`
- `.planning/REQUIREMENTS.md` — New `### Button State Wiring (BTN-STATE-*)` section + 2 Pending traceability rows

## Decisions Made

- BTN-STATE-01 VariantsMapper.CanMap intentionally starts RED: the D-01 `!Layer.ParsedTags.HasType()` guard is NOT yet in production code — this is a feature of the plan, not a bug. Plan 02 turns it GREEN.
- BTN-STATE-02 tests call mappers directly (no FLayerMappingRegistry round-trip) per D-04 pattern to avoid TArray::Sort introsort non-determinism at same priority tier.
- Child ParsedTags populated via `PopulateParsedTagsRecursive(Layer.Children)` after `AddStateChild` calls — necessary because `MakeTaggedTestLayer` only populates tags on the parent layer (Pitfall 5 mitigation from TestHelpers.h).
- No `TestHasLog` or log-capture assertions per Pitfall 4 — slot-population state verified via `GetResourceObject() != nullptr` / `== nullptr`.

## Deviations from Plan

None — plan executed exactly as written. Task 1 file content matches the exact cpp block specified in `<action>`. Task 2 REQUIREMENTS.md edits match the exact insertion text specified.

## Issues Encountered

- `.planning/REQUIREMENTS.md` is gitignored but tracked via force-add (pre-existing pattern from Phase 16.1). Used `git add -f .planning/REQUIREMENTS.md` for Task 2 commit — consistent with all prior .planning edits.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plan 02 (Wave 2 GREEN): Apply `FVariantsSuffixMapper::CanMap` one-line D-01 guard (`&& !Layer.ParsedTags.HasType()`), add D-03 aggregate missing-slot warning to `FButtonLayerMapper::Map`, and flip both REQUIREMENTS.md checkboxes to `[x]` + Traceability rows to `Complete`.
- BTN-STATE-01 VariantsMapper.CanMap assertion is currently RED — expected until Plan 02. All other assertions (ButtonMapper.CanMap accepts, parsed tags, bare @variants pass-through) should compile and pass.
- No compiler/build gotchas encountered in this plan. Open Question 1 (`TFunctionRef` type alias) is not relevant to this spec file — may surface in Plan 02 if it modifies FButtonLayerMapper internals.

## Known Stubs

None — this plan creates test scaffolding only. No data-flow stubs or UI placeholders.

---
*Phase: 17.1-button-variants-state-wiring*
*Completed: 2026-04-22*
